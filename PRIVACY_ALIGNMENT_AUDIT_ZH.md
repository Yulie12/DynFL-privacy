# 隐私逻辑核对与修改边界

本次核对基于 v28 工作区，不代表现有实验已经通过完整隐私验证。既有结果保留，不修改预算、学习率、候选集合或训练轮数。

## 已确认的问题

| 问题 | 证据位置 | 处理状态 |
| --- | --- | --- |
| DP 会计值被误认为完整训练记录的保证 | main.tex 的 Privacy Budget Accounting；训练摘要 uniform_end_to_end_update_dp | 已收窄论文表述，旧摘要键返回空值，另记机制覆盖情况 |
| 纯 HE 的零 DP 事件被理解为零隐私泄露 | privacy.py 会计器；Ours 的 update_eps=0 | 日志明确仅统计已登记的 DP 事件 |
| 真实密码运算与进程隔离混淆 | main.tex 的 CKKS 附录；he_backend.py | 已区分设计角色、单进程执行与 profiled 明文聚合 |
| 组合 DP 的聚合噪声与选择器旧代价不同 | fmnist_lenet5_dynamic.py 的 _distributed_aggregate_dp_parameters；selection.py 的 _cached_local_omega_components | 尚未改完，需要用候选参与集合和实际权重核对，不能简单除以客户端数 |
| 裁剪阈值与有效更新尺度未校准 | 当前 C=1，原实验未触发裁剪，噪声远大于有效更新 | 已增加客户端裁剪前范数诊断，尚未改变阈值 |
| 聚合噪声参数未拒绝非法输入 | _distributed_aggregate_dp_parameters | 已增加有限性、范围和溢出检查及不等权方差测试 |
| 历史非 DP 发布不能由后续 DP 发布补救 | 混合机制跨轮切换 | 已增加覆盖测试，未声称有完整模型 DP 保证 |

## 需要用户决定的保护目标

统一目标要求所有发布结果满足客户端级 DP。对于不受信聚合器，HE 与聚合 DP 配合使用。纯 HE 不作为满足发布 DP 要求的替代路径。该选择会改变候选可行集、论文定义与实验结果。

分级目标保留 DP、HE 和组合机制，但按保护对象和攻击者区分要求，不能把所有候选当作具有同一 DP 保证。该选择需要重新定义风险约束与公平比较条件。

用户决定之前，不强制使用 DP，不删除纯 HE 候选，不更改现有正式实验配置。

## 聚合 DP 的待核对条件

固定公开参与集合和权重下，客户端贡献先按 C 裁剪。若某客户端只出现在一个包中，其全局权重是包归一化权重与包内客户端占比的乘积。一次聚合发布的替换敏感度可据此界定为 2C 乘以受保护客户端最大权重。

当前独立噪声份额的加权方差测试只证明数值关系。还需要核对以下条件。

- 一个客户端是否在多个发布或多个包中重复出现，是否需要累计其影响。
- 参与集合、样本数量与权重是否确实属于公开固定信息。
- 本地持久模型、共享边缘模型与历史发布是否使其他客户端的输出也依赖被替换的数据。
- 解密方、聚合方和噪声生成方能看到什么，是否存在串谋或份额泄露。
- 任何一方是否能先获得无噪声聚合结果。
- 选择器是否使用未经保护的数据相关诊断来决定公开路径。
- 实验使用公开种子复现噪声，不能将这种随机数设置当作部署安全保证。

若这些条件未闭合，RDP 数值本身不能证明完整训练记录的 DP。

## 新日志含义

privacy_execution_audit 分开记录 DP 事件会计范围、选择覆盖、真实或模拟 HE 轮数、单进程密钥作用域和实验随机数作用域。end_to_end_dp_status 暂为 not_established，end_to_end_epsilon 为空。

update_dp_preclip_norm_count、mean、max、p50 和 p90 描述实际进入 DP 裁剪路径的客户端更新，不能代表未选择 DP 客户端的总体分布，也不自动用于改变下一轮阈值。这些原始统计属于内部诊断数据，不属于已证明受 DP 保护的公开输出。

## 下一步顺序

1. 确定统一或分级保护目标。
2. 对实际发布与 RDP 事件逐项匹配，处理权重和持久状态依赖。
3. 用同一参与集合与权重计算执行噪声和选择器代价，并回归测试增量搜索与完整评价。
4. 固定拓扑、数据与初始化做短程效用诊断，再按明确的公开或隐私校准程序决定裁剪阈值。
5. 验证真实 HE 运算及角色边界，再扩展正式多方法实验。
6. 最后同步 TeX 的完整机制公式和实验表图，不沿用此前不同保护目标的结果。

## 参考依据

后续设计优先依据[近期文献核对记录](RECENT_PRIVACY_LITERATURE_REVIEW_ZH.md)，其中区分了已读取全文与仅有摘要摘录的来源，并记录了保护粒度与代码适用条件。以下较早文献仅保留为基础定义来源。

[Mironov 的 RDP 论文](https://arxiv.org/abs/1702.07476)给出 RDP 与组合分析基础。

[McMahan 等人的客户端级 DP 训练研究](https://arxiv.org/abs/1710.06963)展示客户端级保护的训练方法；其结果不直接保证本仓库的小规模、高维更新配置具有相同效用。
