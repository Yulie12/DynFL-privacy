# 隐私机制实验验证顺序

先验证机制与训练流程，再对齐 TeX。不修改旧实验数据，不以追平无保护精度作为隐私机制通过标准。

## 已固定的目标

按客户端分配 CIFAR 数据，保护整个客户端数据集的替换。客户端名单固定，不宣称隐藏参与行为，也不称为自然人用户级保护。端与关联边缘受信，云不受信。发布给云或其他对手的新结果需要纳入保护分析。

## 第一阶段 数值实现

用同一批公开合成更新，对照明文聚合与真实 HE 聚合，然后加同一份噪声。全参数加密，不允许用开销模型替代真实运算。

2026 年 9 月 12 日验证通过。100 客户端，每个 5130 参数，裁剪阈值 0.1，SEAL 加密聚合最大绝对误差约 7.6e-8，事先设置容差 1e-5。聚合耗时约 0.70 秒。脚本为 experiments/validate_paired_he_release.py。

这不是训练实验或协议证明。单进程持有私钥，未强制限制解密对象；固定噪声种子只用于公开数据的数值测试。该容差不是 DP 安全证明中的数值近似界。

## 第二阶段 发布流程

用户已选择由各受信边缘负责发布，不增加全局受信角色。客户端只信任所属边缘，不能查看其他客户端的原始更新；边缘可以查看本组更新。云诚实但好奇，只接收经过 DP 的边缘结果，或这些结果的密文。客户端与边缘之间仍需认证、访问控制与加密传输，受信处理不等于允许明文暴露给旁观者。

固定架构流程为逐客户端训练、所属边缘逐客户端裁剪、本组加权聚合、边缘加 Gaussian 噪声、可选 HE 加密、云聚合、返回全局模型。任何未保护的边缘聚合结果不得先交给云。

第一批验证固定客户端名单和所属边缘，以固定公开样本配额确定权重。替换一个客户端的数据时不改变这些配额。若以后使用私有样本量或数据驱动参与集合，必须另行分析，不能复用固定权重结论。

设组内固定归一化权重为 a_i。给定此前已保护的公开模型，各客户端只依赖自己的数据计算更新，并分别裁剪到 C 时，边缘发布的替换敏感度为 2C max_i a_i。这个界来自仅有一个裁剪贡献变化且其差范数至多 2C。它不是适用于任意共享边缘训练状态的通用公式。若一个客户端的数据会改变组内多个更新，应按受影响权重总和重新计算。

云能够看到每个边缘 DP 结果，因此分别按这些可见发布校准。固定互斥分组且不存在跨组额外私有依赖时，同轮每个客户端仅消耗所属边缘发布的事件预算，不机械乘以边缘数。跨轮继续组合；若迁移或同一客户端影响多个可见发布，则需跟踪所有相关事件。全局模型对已保护结果的固定聚合属于后处理，不能把同一结果的转发重复记作独立加噪事件。

正式合规候选是 DP 和 DP+HE。无保护和纯 HE 可以作为诊断基线，但不满足本次确定的发布结果 DP 目标。预算不足时不能退回纯 HE，应停止新的相关私有发布或采用经过证明且仍满足预算的发布计划。

HE 对 DP 后的边缘结果提供额外机密性，不降低既定的发布 DP 记账。若希望云不能读取单个边缘结果，则云不能持有可任意解密的私钥，必须规定只允许解密合法聚合结果的协议。该部分尚未实现，不将真实 SEAL 运算误写为安全协议已完成。

此前全局受信聚合诊断保留为参考实验，不称为效用上界。在等大分组、组内等权、组间等权、相同 C 和噪声乘数、各边缘独立加噪的条件下，E 个边缘聚合后的每坐标噪声标准差为全局一次加噪的 sqrt(E) 倍。这是该条件下的噪声尺度关系，不是精度上下界。

不得通过哪种方案精度更高来暗中改变信任假设。不得让不受信云先看到未保护结果，再声称后续加噪保护了它此前看到的信息。

检查通过标准包括保护单位、相邻关系、每次可见输出、敏感度、噪声标准差和预算事件一一对应。固定公共权重、参与集合，禁止直接套用 2C/N 到共享私有状态的流程。

## 第三阶段 最小训练对照

比较无保护、仅裁剪、聚合后 DP、真实 HE 聚合后 DP。数据划分、模型、初始化、训练参数、参与集合和权重相同。先复用现有分类头诊断，避免重复调参；之后才扩展到正式训练参数规模。

相同更新和相同噪声的配对实验用于定位 HE 数值误差。独立训练轨迹的偏离需另行分析，不能要求多轮训练逐位相等。预先固定轮数、裁剪阈值和预算，不根据私有诊断临时改变发布规则而漏记开销。

记录精度、损失、裁剪比例、有效更新范数、噪声范数、事件数、累计预算及分阶段实际时间。无保护和内部诊断仅用于研究，不属于已证明安全的公开发布集合。

效用评价应与相同保护单位、信任边界和预算的方案比较。允许隐私效用损失，但报告后期退化与最佳轮次，不仅选取最高精度。

## 第四阶段 动态方法

接入共享边缘状态与动态拓扑后，重新分析依赖和敏感度。隐私选择及其可见日志若依赖私有数据，也必须分析。每条候选路径都应满足既定发布保护目标，不能以纯 HE 替代发布 DP。

用最小规模真实训练验证后，再运行完整模型和多种子实验。最后依据实际协议更新 TeX 的信任模型、噪声公式、预算计算和成本模型。

## 固定边缘诊断已实现

脚本 experiments/validate_edge_dp_trajectory.py 独立于正式训练入口，不改旧配置或 TeX。默认 CIFAR10、预训练 ResNet18 冻结主体仅训练分类头、6000 训练样本、200 测试样本、固定随机分配、100 客户端和 10 边缘。按固定样本配额计算组内权重，客户端按编号轮流分到边缘，每轮从同一已发布全局模型独立训练，不复用跨客户端私有边缘状态。

每轮日志含各边缘最大权重、敏感度、噪声标准差、裁剪比例，全部客户端累计 epsilon、每客户端事件数和实际分阶段耗时。所有范数与对照指标仅为内部诊断数据，不宣称它们可以无额外保护地公开。受信边缘和云用同一进程模拟，尚未实现网络访问控制或真实隔离，也未接入 HE 和动态策略。

复现本次短程验证，在仓库根目录运行以下单行命令。每次生成新结果目录，不覆盖旧结果。

```powershell
D:\soft\Python310\python.exe experiments\validate_edge_dp_trajectory.py --rounds 5 --privacy-horizon 100 --clients 100 --edges 10 --clip-norm 0.1 --epsilon 8 --seed 42 --output-root out/trusted_edge_dp_5rounds_seed42
```

2026 年 9 月 12 日完成三条路径各 5 轮，共 15 条记录。噪声按 100 轮总预算 8 校准，不是按 5 轮校准。45 项边缘、单轮与轨迹相关测试通过。

| 路径 | 第5轮精度 | 第5轮损失 | 每客户端累计 epsilon |
| --- | --- | --- | --- |
| 无保护 | 39.0% | 1.8405 | 不适用 |
| 仅裁剪 | 29.5% | 1.9850 | 不适用 |
| 边缘聚合后 DP | 14.5% | 3.4723 | 1.6050 |

每组 10 客户端，组内最大权重 0.1，裁剪阈值 0.1，敏感度为 0.02；噪声乘数约 6.9112，每边缘每坐标噪声标准差约 0.138224。十组等权聚合后的标准差约 0.043710，比同条件全局一次加噪约大 sqrt(10) 倍。每客户端五轮记录五个事件，不是五十个。

发布顺序和记账的实现检查通过，但效用尚未通过。DP 路径损失从约 2.5982 增至 3.4723，不能只因精度超过随机水平就称为正常收敛，也不能用五轮判断最终精度。尚不建议据此启动正式 100 轮全方法实验。下一阶段应预先确定同等隐私目标下的诊断问题，不私自放宽保护单位、提高预算或减少边缘数以取得更好精度。

原始结果位于 out/trusted_edge_dp_5rounds_seed42/2026-09-12_15-03-32_edge_dp_trajectory/report.json。

## 分布式噪声与真实 HE 的条件诊断

新增 --dp-release distributed_he，默认仍为 edge_local，不改变既有命令。该诊断预先限定全部边缘在线、无边缘向云泄露噪声份额。每个边缘在加密前加入自己的独立噪声份额，全部边缘结果用 SEAL 全参数加密聚合，解密输出已经带噪声。不允许漏掉边缘后悄悄重新归一化。

组间固定权重为 b_j，全局目标噪声标准差为 s，最低未知噪声边缘数为 H 时，各边缘加权前噪声标准差为 s/(sqrt(H)*b_j)。至少 H 份独立未知噪声保留，才达到该全局目标方差。当前训练诊断使用 H=E，E 为全部边缘数；H<E 仅有条件性数学测试，不是已经实现抗串谋或掉线协议。全部 E 份保留时实际噪声标准差为 s*sqrt(E/H)。

此处分配降低了单个边缘输入的噪声，它不再独立满足原目标预算，必须隐藏其明文。每客户端按全局发布事件和全局噪声乘数记账，不按局部噪声份额乘数计账。共享私有边缘状态、私有选择信息、私有权重和其他发布仍未包含在该诊断保证中。

重要限制是计算仍在一个研究进程中执行，进程持有完整私钥且可访问原始更新、噪声份额和明文对照。尚无门限解密或密钥隔离。因此它验证噪声计算、训练效用与真实 HE 运算，不证明云角色隔离，也不构成生产协议的 DP 证明。所有噪声种子和诊断指标均为内部研究信息。

2026 年 9 月 12 日 5 轮结果如下，仍按 100 轮总预算 8 校准，模型和训练参数与边缘独立 DP 保持一致。

| 路径 | 第5轮精度 | 第5轮损失 | 云聚合噪声标准差 |
| --- | --- | --- | --- |
| 无保护 | 39.0% | 1.8405 | 0 |
| 仅裁剪 | 29.5% | 1.9850 | 0 |
| 边缘独立 DP，前次诊断 | 14.5% | 3.4723 | 0.043710 |
| 分布式 DP 与真实 HE | 23.0% | 2.1266 | 0.013822 |

两条 DP 路径第5轮每客户端 epsilon 均约 1.6050。新路径每轮 10 个边缘各加密 5130 个参数，共 51300 个参数值；五轮相对于同输入明文加权聚合的最大绝对误差约 3.02e-8。三条路径五轮总运行时间约 31.26 秒，不含前置数据和模型加载。

这种改善不意味着已达到全局可信诊断的同一精度。噪声分布尺度可相同，但随机实现与轨迹不必相同，也没有比较多个种子。保持参数不变继续20轮用于检查学习走势，不把5轮效果当作最终论文结论。

```powershell
D:\soft\Python310\python.exe experiments\validate_edge_dp_trajectory.py --dp-release distributed_he --rounds 20 --privacy-horizon 100 --clients 100 --edges 10 --clip-norm 0.1 --epsilon 8 --seed 42 --output-root out/distributed_edge_dp_he_20rounds_seed42
```

五轮原始记录为 out/distributed_edge_dp_he_5rounds_seed42/2026-09-12_16-42-48_edge_dp_trajectory/report.json。

### 同参数20轮验证

保持相同数据、初始化、裁剪阈值、学习率和100轮预算校准，三条路径各运行20轮，全部完成。前5轮精度与短程结果一致，损失差异小于1e-5。

| 路径 | 第20轮精度 | 第20轮损失 | 每客户端累计 epsilon |
| --- | --- | --- | --- |
| 无保护 | 46.0% | 1.4612 | 不适用 |
| 仅裁剪 | 46.0% | 1.5350 | 不适用 |
| 分布式 DP 与真实 HE | 37.5% | 1.8036 | 3.3146 |

分布式路径损失从第1轮2.3261降至第20轮1.8036，存在逐轮波动。结果支持该固定独立客户端分类头诊断下可学习，不代表100轮稳定、完整ResNet训练或动态协作机制已经通过。与无保护仍相差8.5个百分点，这是本次观察到的效用代价，不要求其消失才算机制正确。

全程每轮真实加密10个边缘的全部5130维贡献，未使用模拟HE或部分参数加密。20轮最大配对聚合绝对误差约3.30e-8，HE聚合累计约1.29秒。三条训练路径总耗时约124.95秒，不含前置加载。该开销是小维度单进程诊断，不能外推为完整模型或分布式部署开销。

共60项相关测试通过。日志检查覆盖全部60条结果、每客户端事件数、预算、加密覆盖和同配置前缀重现。尚未做单进程之外的密钥隔离、门限解密、串谋防护、掉线恢复或有限精度DP证明，TeX正式机制与主实验结果未替换。

20轮原始记录为 out/distributed_edge_dp_he_20rounds_seed42/2026-09-12_16-44-33_edge_dp_trajectory/report.json。

### HE 发布权限反例审计

2026 年 9 月 12 日新增 experiments/audit_he_release_boundary.py，以公开合成数据实测当前接口权限，不使用训练原始数据。

- 同一 SEAL 运行环境具有完整私钥，能够解密单个密文，实测最大绝对误差约1.28e-9。
- 给定完整名单时，缺少边缘输入会被拒绝。但调用方可改传单边缘名单与权重1，接口会接受并返回该边缘输入。因此现有名单检查只是算术输入校验，不能当成授权或防子集解密机制。
- 目前没有可信方绑定的轮次与参与集合，也没有重放拒绝。这不代表已经发生远程攻击，而是说明本地数值接口不应直接暴露为云可调用的解密服务。

完整协议仍需由明确的可信角色绑定任务、轮次、参与集合、权重、数据布局和获准密文，并限制每次发布。普通字段比对无法阻止持有完整私钥的主体绕过接口解密。

已向用户提出必要信任选择。指定受信边缘保管完整私钥，需要额外信任该节点不解密单个边缘贡献；联合门限密钥则需要明确参与和串谋门限，并采用经过验证的多方密码实现。当前未擅自选择或引入新库，既有正式配置与 TeX 不变。

审计原始结果为 out/he_release_boundary_audit/2026-09-12_16-54-30_he_boundary/report.json，状态为 audit_completed_protocol_not_ready，不将测试执行成功误写为安全协议通过。

## 已选择指定受信边缘持有完整私钥

用户明确选择指定受信边缘保管完整私钥。它是额外承担全局解密授权职责的受信节点，云不能获得私钥。必须信任它不绕过授权接口解密单个边缘贡献，不宣称对该节点本身提供密码学限制或实现了门限解密。

新增 experiments/trusted_edge_custodian.py 与 experiments/he_cloud_worker.py。诊断以边缘0为保管者，身份不改变训练权重。每个保管会话固定参与边缘与权重，生成独立任务标识和密钥，私钥仅留在受信进程内存中，不写入交换目录。云子进程只获得密文和公开任务清单，不传递更新明文、噪声份额或密钥。

每轮授权绑定任务、轮次、输入密文摘要、参与边缘、固定权重、维度和输出块数。受信边缘复算完整密文和，并核对云返回密文的摘要；核对通过后解密受信方保留的对应密文和，不直接解密云任意提交的密文。每轮只能释放一次，错轮、缺边缘、替换为单个密文、篡改任务清单和重复释放都拒绝。额外的受信密文复算时间单独记录，不能把该开销隐藏掉。

这些约束是进程与协议接口层面的分离，不是同机操作系统沙箱。所有边缘训练、加噪和公钥加密仍在受信主进程模拟，并没有独立部署所有边缘；尚未实现网络认证、远程传输加密或抵御恶意私钥持有者。研究噪声使用固定种子，完整生产DP与有限精度安全证明仍未建立。

新增 --he-custody trusted_edge 显式启用此路径，原 arithmetic_harness 仍保留用于复现旧实验。正式训练入口和 TeX 未改。可用 --methods 只运行所需诊断路径，不能与 --dp-release 的机制矛盾。

### 权限隔离后的短程验证

五轮全部完成。无保护39.0%、仅裁剪29.5%、分布式DP与真实HE23.0%，与原数值接口的五轮精度一致；后者损失约2.1266，累计epsilon约1.6050。日志确认云与保管者PID不同，每轮加密全部51300个参数值，并启用轮次绑定和重放拒绝。81项相关测试通过，其中10项针对新的保管者接口。

五轮记录为 out/trusted_custodian_dp_he_5rounds_seed42/2026-09-12_17-12-03_edge_dp_trajectory/report.json。

继续100轮仅运行受影响的DP与HE路径，固定其余参数，不重复训练对照。命令如下。

```powershell
D:\soft\Python310\python.exe experiments\validate_edge_dp_trajectory.py --dp-release distributed_he --he-custody trusted_edge --methods distributed_dp_he --rounds 100 --privacy-horizon 100 --clients 100 --edges 10 --clip-norm 0.1 --epsilon 8 --seed 42 --output-root out/trusted_custodian_dp_he_100rounds_seed42
```

### 受信私钥保管者路径100轮完成

2026年9月12日完成100轮，固定seed42、100客户端、10边缘、C=0.1和100次发布预算8。未改学习率、未按中途精度调整噪声、未切换为纯HE。仍然是6000训练样本、200测试样本、固定随机分配和5130维分类头诊断，不是完整模型或动态拓扑实验。

| 指标 | 结果 |
| --- | --- |
| 最终精度 | 43.0% |
| 最高精度 | 48.5%，第85轮 |
| 最后10轮平均精度 | 44.7% |
| 最终损失 | 1.8653 |
| 最低损失 | 1.6853，第64轮 |
| 每客户端发布事件数 | 100 |
| 最终累计epsilon | 8.0 |
| 云聚合噪声标准差 | 0.013822 |
| 真实HE最大配对绝对误差 | 约3.69e-8 |

全部100条记录检查通过，云与受信保管进程不同，未向云传递私钥，任务和轮次绑定一致，单轮加密51300个参数值且仅放行一次授权结果。前20轮精度与此前数值接口一致，损失差异小于1e-5。86项相关测试通过。

本次DP路径总运行时间约244.45秒，不含前置加载；其中HE接口累计约37.23秒，云子进程调用约28.40秒，受信侧密文加法复核约0.083秒。这些计时存在包含关系，不可相加。每轮重新启动轻量云进程，包含启动、密文文件交换和验证开销。运行期间有单元测试，当前时间用于诊断，不作为严格公平的性能基准。

结果支持该固定条件下可学习和100轮正常完成，没有重现接近随机猜测的严重失效。但后期精度波动、损失反弹仍存在，不能声称稳定收敛或与无保护等效。本次没有重跑100轮对照，不把不同运行的结果未经配置核对直接合并为正式表格。

还没有完成网络部署认证、独立边缘进程、生产随机性、有限精度DP论证、动态候选的数据依赖分析或完整训练参数规模验证。受信保管者拥有完整私钥是明确假设，不是被密码学禁止解密单个贡献。TeX主结果未替换。

原始结果为 out/trusted_custodian_dp_he_100rounds_seed42/2026-09-12_17-13-35_edge_dp_trajectory/report.json。

## 三种可训练范围的效用参考诊断

用户新增 resnet18_pretrained_layer4_head，保持其实现，不替换用户补丁。已检查模型构造、冻结策略、优化器与批大小，补充单轮诊断入口支持和实际本地训练测试。新范围只更新layer4与分类头，冻结参数和BatchNorm缓冲区在测试训练中保持不变。

| 范围 | 实际可训练参数 | 同标准差下噪声范数均方根 |
| --- | --- | --- |
| 仅分类头 | 5130 | 约0.990 |
| layer4与分类头 | 8393738 | 约40.04 |
| layer3、layer4与分类头 | 10490890 | 约44.77 |

均方根使用每坐标标准差0.0138224乘以维度平方根。中间范围参数减少约20%，整体噪声均方根仅减少约10.6%。这是矩计算，不是效用定理。三种范围同时改变了可优化层与参数维度，不能把精度差异全部归因于维度。

### 同条件第5轮对照

三个范围均为CIFAR10、100客户端、6000训练样本、200测试样本、固定随机划分、seed42、3本地epoch、学习率0.01、C=0.1、100轮预算8。此处使用全局受信聚合的效用参考，不使用HE，也不作为部署安全证据。分类头取已有100轮同配置轨迹的前5轮，预算校准相同；不能与分布式噪声不同随机实现的五轮23.0%混淆。

| 范围 | 无保护精度 | 仅裁剪精度 | 聚合DP精度 | 聚合DP损失 |
| --- | --- | --- | --- | --- |
| 仅分类头 | 39.0% | 29.5% | 24.5% | 2.0377 |
| layer4与分类头 | 48.0% | 38.5% | 14.5% | 3.0159 |
| layer3、layer4与分类头 | 52.0% | 39.0% | 10.5% | 3.6867 |

三种DP路径第5轮累计epsilon均约1.605。中间范围噪声范数实测约40.05，原范围约44.77。中间范围DP损失从约2.562升至3.016，原范围从约2.498升至3.687。新范围有一定改善，但五轮不足以认定可稳定学习或彻底失败。

已有正式范围单轮三噪声试验的聚合DP精度为12.5%至15.5%。无保护32.5%、仅裁剪13.0%。该单轮参考的随机流与多轮轨迹不同，不将它们拼接成同一条曲线。

多轮参考脚本新增可选 --methods，用于只延长所需路径；默认仍运行全部对照。保持参数不变，将中间范围DP路径单独验证20轮，不重复训练对照。

```powershell
D:\soft\Python310\python.exe experiments\validate_trusted_aggregate_trajectory.py --model resnet18_pretrained_layer4_head --methods trusted_aggregate_dp --rounds 20 --privacy-horizon 100 --clients 100 --train-limit 6000 --test-limit 200 --local-epochs 3 --lr 0.01 --epsilon 8 --delta 1e-5 --clip-norms 0.1 --seed 42 --device cuda --output-root out/layer4_head_scope_dp_20rounds_seed42
```

原始记录位于 out/full_trainable_scope_dp_single_round/2026-09-12_18-10-43_trusted_aggregate/report.json、out/full_trainable_scope_dp_5rounds_seed42/2026-09-12_18-12-04_trajectory/report.json 和 out/layer4_head_scope_dp_5rounds_seed42/2026-09-12_21-37-31_trajectory/report.json。未修改TeX和正式实验配置。

### 中间范围20轮结果

中间范围聚合DP单独完成20轮。最终精度17.5%，损失14.2031，累计epsilon约3.3146，每轮噪声范数约40.05。前5轮与短程诊断一致。所有记录维度均为8393738，事件数与轮次一致。

这组结果表明新增范围没有解决当前配置下的效用问题，但不证明所有参数或其他隐私机制下都无法训练。代码中的冻结范围、BatchNorm状态、本地训练实际更新键与维度检查通过；相关测试合计92项通过。没有把原范围的私有更新留在模型中而只对分类头加噪，也没有为了提高精度放宽预算。

该诊断不使用HE，不能归因为加密误差。它同时改变训练范围和维度，不构成纯维度效应的因果验证。新范围可保留为训练范围对照点，不直接替换正式模型或默认配置。进一步改变可训练参数化需要用户选择；尚未引入低秩适配器或改变任何基线。

结果位于 out/layer4_head_scope_dp_20rounds_seed42/2026-09-12_21-40-11_trajectory/report.json。

## 低维固定投影适配模块诊断

### 100轮真实HE补充结果

适配模块已接入 `validate_edge_dp_trajectory.py` 的独立模型选项，默认仍为head。使用分布式噪声、真实SEAL、指定受信edge0保管完整私钥，完成100轮。预算、C、客户端与数据规模沿用20轮诊断，未放宽隐私目标。

最终精度38.5%，最好42.5%，最后10轮均值37.7%，最终loss1.8682，累计epsilon8。每轮10个边缘各加密9738个参数值，最多解密误差4.136e-8。整条轨迹实测264.06秒，其中HE接口42.83秒已包含在总时间中，不能重复相加。这是单机诊断时间，不是正式系统时延基准。

没有出现高维实验中的严重损失爆炸，但后段有回退。此前相同受信保管流程的head参考最终43.0%、最好48.5%、最后10轮44.7%。当前结果不支持适配模块优于head，不自动替换正式模型。不同维度的噪声实现也不同，单种子不能证明普遍排序。上一条全局可信聚合20轮40.5%与此次分布式噪声路径不共享同一噪声样本，不能将差异归因于HE。

冻结参数实际训练检查等相关测试95项通过。完整私钥保管者必须受信；独立云进程并不提供同主机恶意进程下的OS隔离。噪声种子与诊断信息仍仅用于内部研究，动态模式尚未接入此隐私发布协议。

结果位于 `out/adapter_trusted_custodian_dp_he_100rounds_seed42/2026-09-12_22-15-20_edge_dp_trajectory/report.json`。

```powershell
D:\soft\Python310\python.exe experiments\validate_edge_dp_trajectory.py --model resnet18_pretrained_adapter --rounds 100 --privacy-horizon 100 --dp-release distributed_he --he-custody trusted_edge --methods distributed_dp_he --output-root out/adapter_trusted_custodian_dp_he_100rounds_seed42
```

### 初始20轮配对诊断

用户同意低维私有微调方向后，新增独立选项 `resnet18_pretrained_adapter`，没有替换正式配置。公开预训练骨干与 BatchNorm 状态冻结，在池化后加入残差模块 `x + U relu(Px) + b`。P 是与私有数据无关、种子1729生成的固定8维投影；U和b零初始化，与分类头一起训练，共9738个参数。模块不是现成论文方法的完整复现，不声称增加新的隐私定理或算法创新。

所有可训练参数的客户端差分共同裁剪与加噪，固定投影不更新。拆分与完整前向、冻结范围、零初始化恒等映射、随机数状态检查通过，相关回归测试94项通过。该配置的终端骨干冻结，尚未证明它保留正式动态协作各模式的全部训练价值。

配对诊断使用100客户端、6000训练样本、200测试样本、seed42、20轮、隐私校准周期100轮、epsilon8、delta1e-5、C0.1、学习率0.01和本地3轮。三条轨迹从同一个初始模型独立运行。

| 路径 | 第20轮精度 | 第20轮损失 |
| --- | --- | --- |
| 无保护 | 46.0% | 1.4607 |
| 仅裁剪 | 46.0% | 1.5347 |
| 聚合DP | 40.5% | 1.7618 |

DP累计epsilon为3.3146，事件数20，单坐标噪声标准差0.0138224，第20轮噪声范数1.3769。此次诊断未使用HE。结果支持继续进行长期及真实HE验证，不证明多种子优势、100轮稳定收敛或正式动态协议隐私闭合。所有控制结果、原始范数和可复现噪声种子均属于内部诊断，不应当作联合公开的DP输出。

结果位于 `out/adapter_scope_dp_20rounds_seed42/2026-09-12_22-09-33_trajectory/report.json`。

复现命令如下。

```powershell
D:\soft\Python310\python.exe experiments\validate_trusted_aggregate_trajectory.py --model resnet18_pretrained_adapter --rounds 20 --privacy-horizon 100 --clip-norms 0.1 --output-root out/adapter_scope_dp_20rounds_seed42
```
