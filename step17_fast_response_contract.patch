diff --git a/configs/paper_v31_cifar10_resnet18_fast_response.json b/configs/paper_v31_cifar10_resnet18_fast_response.json
new file mode 100644
index 0000000..5f34487
--- /dev/null
+++ b/configs/paper_v31_cifar10_resnet18_fast_response.json
@@ -0,0 +1,117 @@
+{
+  "name": "paper_v31_cifar10_resnet18_fast_response",
+  "execution_revision": "paper_flow_v31_mode_specific_privacy",
+  "output_root": "out/paper_v31_final/fig4_fast_response",
+  "seeds": [
+    40,
+    42,
+    44
+  ],
+  "policies": [
+    "fixed_mode_fixed_privacy",
+    "dynamic_mode_fixed_privacy",
+    "fixed_mode_dynamic_privacy",
+    "full_dynfl"
+  ],
+  "training": {
+    "dataset": "cifar10",
+    "model": "resnet18_pretrained_head",
+    "rounds": 100,
+    "train_limit": 12000,
+    "test_limit": 2000,
+    "partition_mode": "edge_label_skew",
+    "local_epochs": 3,
+    "learning_rate": 0.01,
+    "device": "cuda",
+    "executor": "serial",
+    "selection_period": 1
+  },
+  "system": {
+    "clients": 100,
+    "edges": 10,
+    "resource_limit": 1.35,
+    "memory_limit": 1.35,
+    "time_limit": 8,
+    "risk_limit": 0.5,
+    "aggregation_fraction": 1.0,
+    "client_heterogeneity": 2,
+    "edge_heterogeneity": 1.5,
+    "require_feasible": true,
+    "require_edge_cloud_coverage": true,
+    "min_edge_cloud_fusion_ratio": 0.5,
+    "trusted_edge_split_execution": false,
+    "mainline_fusion": false,
+    "fast_client_deadlines": {
+      "0": 20.0,
+      "1": 20.0,
+      "2": 20.0,
+      "3": 20.0,
+      "4": 20.0,
+      "5": 20.0,
+      "6": 20.0,
+      "7": 20.0,
+      "8": 20.0,
+      "9": 20.0,
+      "10": 20.0,
+      "11": 20.0,
+      "12": 20.0,
+      "13": 20.0,
+      "14": 20.0,
+      "15": 20.0,
+      "16": 20.0,
+      "17": 20.0,
+      "18": 20.0,
+      "19": 20.0
+    }
+  },
+  "network": {
+    "end_edge_rate_mb_s": 5,
+    "end_cloud_rate_mb_s": 2.2,
+    "edge_cloud_rate_mb_s": 8,
+    "end_edge_base_latency_sec": 0.015,
+    "end_cloud_base_latency_sec": 0.04,
+    "edge_cloud_base_latency_sec": 0.01,
+    "periodic_amplitude": 0.2,
+    "period_rounds": 3,
+    "jitter": 0.25
+  },
+  "privacy": {
+    "protection_scope": "cross_domain_update_and_secure_aggregate",
+    "candidate_mechanisms": [
+      "dp",
+      "he3",
+      "dp_he3"
+    ],
+    "accounting_mode": "rdp_auto",
+    "initial_epsilon": 8,
+    "update_epsilon_budget": 8,
+    "delta": 1e-05,
+    "clip_norm": 0.25,
+    "update_mode": "upd_only",
+    "enforce_cloud_dp_stability": false,
+    "cloud_dp_stability_threshold": 1,
+    "release_calibration": "tex_packet",
+    "update_protection_goal": "packet_protection"
+  },
+  "he": {
+    "backend": "seal",
+    "execution": "real",
+    "require_real_he": true,
+    "aggregation_size": 0,
+    "workers": 4,
+    "poly_modulus_degree": 8192,
+    "coeff_modulus_bits": [
+      50,
+      40
+    ],
+    "scale_bits": 40
+  },
+  "optimization": {
+    "pareto_archive_size": 16,
+    "pareto_beam_size": 4,
+    "pareto_max_iters": 50,
+    "pareto_neighbor_top_k": 0,
+    "cloud_fusion_xi": 0.2,
+    "cloud_fusion_eps": 0.05
+  }
+}
diff --git a/experiments/run_final_paper_suite.py b/experiments/run_final_paper_suite.py
index 38b4d80..0a618f8 100644
--- a/experiments/run_final_paper_suite.py
+++ b/experiments/run_final_paper_suite.py
@@ -21,6 +21,8 @@ if str(ROOT) not in sys.path:
 from experiments.paper_final_plan import FORMAL_SEEDS, RESOURCE_SCENARIOS, STRATEGY_PERIOD_VALUES
 from experiments.run_paper_config import DEFAULT_CONFIG, build_command, validate_config
 
+DEFAULT_FAST_CONFIG = ROOT / "configs" / "paper_v31_cifar10_resnet18_fast_response.json"
+
 FINAL_STUDIES = ("main", "dynamic_resources", "privacy", "fast_response", "optimizer", "sp")
 
 
@@ -30,8 +32,9 @@ def parse_args() -> argparse.Namespace:
     parser.add_argument(
         "--fast-config",
         type=Path,
-        help=("Formal fast-response config. It must explicitly define system.fast_client_deadlines; "
-              "the orchestrator never invents a deadline or a fast-client set."),
+        default=DEFAULT_FAST_CONFIG,
+        help=("Formal fast-response config. Defaults to the frozen Fig.4 contract: "
+              "clients 0-19 (20%% of 100 clients), each with a 20 s hard deadline."),
     )
     parser.add_argument("--studies", nargs="+", choices=FINAL_STUDIES, default=list(FINAL_STUDIES))
     parser.add_argument("--seeds", type=int, nargs="+")
@@ -114,7 +117,7 @@ def main() -> None:
     studies = list(dict.fromkeys(args.studies))
     seeds = list(dict.fromkeys(args.seeds or FORMAL_SEEDS))
     base = _load(args.config)
-    fast = _load(args.fast_config) if args.fast_config else None
+    fast = _load(args.fast_config) if "fast_response" in studies else None
     cases = build_final_cases(base, studies=studies, fast_config=fast)
     rounds = int(args.rounds or base["training"]["rounds"])
     manifest: list[dict[str, Any]] = []
diff --git a/tests/test_paper_conformance.py b/tests/test_paper_conformance.py
index c304862..3b1042f 100644
--- a/tests/test_paper_conformance.py
+++ b/tests/test_paper_conformance.py
@@ -2148,3 +2148,30 @@ def test_multiseed_aggregator_defaults_match_formal_cifar_model() -> None:
     source = Path(aggregate_multiseed_results.__file__).read_text(encoding="utf-8")
     assert 'parser.add_argument("--model", default="resnet18_pretrained_head")' in source
     assert 'reconfiguration_policy = "full_dynfl"' in source
+
+
+def test_frozen_fast_response_config_is_twenty_percent_at_twenty_seconds() -> None:
+    root = Path(__file__).resolve().parents[1]
+    path = root / "configs" / "paper_v31_cifar10_resnet18_fast_response.json"
+    config = json.loads(path.read_text(encoding="utf-8"))
+    deadlines = config["system"]["fast_client_deadlines"]
+    assert config["system"]["clients"] == 100
+    assert len(deadlines) == 20
+    assert set(deadlines) == {str(client_id) for client_id in range(20)}
+    assert set(float(value) for value in deadlines.values()) == {20.0}
+    assert config["seeds"] == [40, 42, 44]
+
+
+def test_final_suite_defaults_to_frozen_fast_response_config() -> None:
+    from experiments.run_final_paper_suite import DEFAULT_FAST_CONFIG, build_final_cases
+    from experiments.run_paper_config import DEFAULT_CONFIG
+
+    base = json.loads(DEFAULT_CONFIG.read_text(encoding="utf-8"))
+    fast = json.loads(DEFAULT_FAST_CONFIG.read_text(encoding="utf-8"))
+    cases = build_final_cases(base, studies=["fast_response"], fast_config=fast)
+    assert len(cases) == 1
+    case = cases[0]
+    assert case["study"] == "fast_response"
+    assert case["config"]["output_root"] == "out/paper_v31_final/fig4_fast_response"
+    assert len(case["config"]["system"]["fast_client_deadlines"]) == 20
+    assert case["policies"] == list(fast["policies"])
