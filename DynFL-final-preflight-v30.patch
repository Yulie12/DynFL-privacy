--- a/tex/paper/main.tex
+++ b/tex/paper/main.tex
@@ -166,7 +166,7 @@
 privacy release contract is fixed independently of the selected collaboration mode.
 The main contributions are summarized as follows.
 \begin{itemize}
-    \item We propose a dynamic cloud-edge-end collaboration reconfiguration framework that adapts execution placement and aggregation topology while preserving one mandatory client-level DP global-release contract.
+    \item We propose a dynamic cloud-edge-end collaboration reconfiguration framework that adapts execution placement and mode-specific logical communication while preserving a fixed trusted-edge global-release topology and one mandatory client-level DP release contract.
     \item We introduce a unified collaboration space representation based on link blocks and link flows. It describes model execution, transmitted objects, aggregation locations, and return paths, while a fixed HE confidentiality layer protects cloud-facing aggregate traffic.
     \item We establish a joint model for latency, communication, encrypted aggregation overhead, and estimated convergence error cost under the common DP release mechanism. HE and DP are modeled with distinct roles rather than as interchangeable privacy choices.
     \item We formulate collaboration reconfiguration as a constrained multiobjective problem. A bounded Pareto search constructs feasible global decisions, and an ideal point Tchebycheff criterion selects the executed configuration.
@@ -425,7 +425,7 @@
 \setlength{\tabcolsep}{4pt}
 \begin{tabular}{lllll}
 \hline
-\textbf{Mode} & \textbf{Execution structure} & \textbf{Aggregation structure} & \textbf{Protected upload} & \textbf{Available mechanism} \\
+\textbf{Mode} & \textbf{Execution structure} & \textbf{Basic-template aggregation} & \textbf{Protected upload} & \textbf{Available mechanism} \\
 \hline
 LIE & End and edge split & Edge & Internal embedding and gradient & Trusted execution + global DP/HE release \\
 LIC & End and cloud split & Cloud & Embedding and gradient & Excluded in this threat model \\
@@ -438,15 +438,12 @@
 \end{tabular}
 \end{table*}
 The corresponding cloud-edge-end collaborative link graphs of the seven modes are illustrated in \figref{fig:eecblockflow}.
-Each subfigure corresponds to one collaborative mode.
+Each subfigure corresponds to one basic collaboration-mode template. These graphs define the mode taxonomy and the mode-specific logical communication charged by the cost model; they do not replace the fixed formal release overlay described above.
 Orange links show link block execution repeated \(L_B\) times.
 Green self loops show the edge aggregation loop before global aggregation, repeated \(E_B\) times.
 Black self loops show the final global aggregation.
 Black arrows show update upload and model return paths among end devices, edge nodes, and the cloud.
-Modes LIE and LIIE finish aggregation at the edge side.
-Modes LIC and LIIC use cloud aggregation directly.
-Mode LIEIIC uploads edge side updates to the cloud and then returns the aggregated model through the edge side.
-Modes LIEIIIC and LIIEIIIC add the \(E_B\) edge aggregation loop before cloud aggregation and final model return.
+In the basic taxonomy, modes LIE and LIIE terminate at the edge side, modes LIC and LIIC use cloud aggregation directly, LIEIIC uploads edge-side updates to the cloud and returns through the edge side, and LIEIIIC and LIIEIIIC include the \(E_B\) edge loop before cloud aggregation. In the formal mainline experiments, these terminal aggregation routes are not executed literally: the selected mode determines computation stages and mode-specific logical communication, while every executable mode joins the same trusted-edge aggregation and single global DP/HE release topology.
 \begin{figure*}[t]
 \centering
 \includegraphics[width=\textwidth]{eecblockflow.png}
@@ -1271,7 +1268,7 @@
 All methods
 use the same data partition, optimizer, epochs per stage, and edge normalized
 aggregation rule in \equref{eq:edge_normalized_fedavg}. The main
-comparison uses three random seeds, 42, 43, and 44. Controlled studies use seeds 40, 42, and 44
+comparison uses three random seeds, 42, 43, and 44. Controlled studies use the same seeds, 42, 43, and 44,
 and change only the parameter or component under evaluation. The key ablation
 uses the three main-comparison seeds 42, 43, and 44.
 
