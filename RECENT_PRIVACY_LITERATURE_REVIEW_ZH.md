# 近期文献与动态隐私保护路径核对

核对日期为 2026 年 9 月 7 日。主要检索窗口为 2023 年 9 月至今。本次没有扩大到近五年。本文是设计核对记录，不是已完成的安全证明或正式实验结论。

## 结论

建议保留云边端协作重构主线，并优先验证统一客户端级 DP 目标下的两条保护路径，即逐包 DP 与 HE 配合聚合 DP。这是结合文献与代码提出的建议，不是某篇论文已经证明本仓库正确。

不把纯 HE 当作相同发布 DP 目标的替代项，不要求每种机制都必须被选中。原候选集合尚未修改，用户尚未明确确认改变最终保护目标。

## 一 近期文献证据

### DDFed，2024，Computers & Electrical Engineering

[出版社页面](https://www.sciencedirect.com/science/article/pii/S004579062400168X)

完整题名为 A federated learning scheme for hierarchical protection and multiple aggregation，作者为 Zhiqiang Wang、Xinyue Yu、Haoyu Wang、Peiyang Xue，DOI 为 10.1016/j.compeleceng.2024.109240。

已读取摘要、引言和章节摘录，未获得全文。其贡献阈值决定更新丢弃、DP 扰动或 HE 加密，两类更新分别聚合，客户端解密并融合；系统含受信密钥生成中心。上述结构支持将其列为直接相关工作，但不能据摘录确认其完整 DP 会计、串谋边界或复现全部算法。

不能用这篇论文作为本仓库统一 DP 保证的证明，也不能根据片段自拟一个阈值算法后标为原版 DDFed。尚需原文的算法、参数与安全分析。

### Heikkila，2025，TMLR

[发表信息](https://researchportal.helsinki.fi/en/publications/on-using-secure-aggregation-in-differentially-private-federated-l/)与[作者全文 v2](https://arxiv.org/html/2407.19286v2)

完整题名为 On Using Secure Aggregation in Differentially Private Federated Learning with Multiple Local Steps。

已核对第 3.2、3.3、4 节和附录 A.3 至 A.5。第 3.2 节式 4 给出客户端级更新裁剪加聚合噪声的参照；主贡献针对更细粒度保护。第 3.3 节假定诚实但好奇的服务器和诚实客户端。若客户端知道并扣除自己的噪声，其余噪声仍需满足目标。附录 A.4 区分理想聚合功能和实际协议，A.5 使用 RDP。

可用于核对发布流程及噪声份额的可见性，不能直接套用到共享未保护模型状态、任意不等权聚合或 CKKS 的数值实现。其安全聚合假设也不等于把训练服务器整体设为受信。

### Robust-HDP，2024，ICML

[发表页面](https://proceedings.mlr.press/v235/malekmohammadi24a.html)与[作者全文 v2](https://arxiv.org/html/2406.03519v2)

完整题名为 Noise-Aware Algorithm for Heterogeneous Differentially Private Federated Learning。

已核对第 2、3.1 至 3.7 节及附录 H。其邻接关系是一个客户端中的一条记录变化，客户端运行逐样本梯度裁剪的 DP-SGD。服务器使用已加噪更新估计噪声并调整权重。附录明确讨论高度异质数据下的限制。

可以借鉴区分预算与实际噪声代价的方法，不能把其样本级 ε 当成本仓库客户端级 ε。其服务器算法需要观察单个已加噪更新，不能直接放入只能看到密文总和的路径。改变聚合权重也会改变现有 TeX 的训练规则，须作为单独方案核对。

### PPML-Hybrid，2025，预印本

[作者全文](https://arxiv.org/html/2511.06064v1)

完整题名为 A Privacy-Preserving Federated Learning Method with Homomorphic Encryption in Omics Data。本次核对的是 arXiv v1，不将其标为已确认同行评审论文。

已读取隐私要求、算法及实验设置。其客户端选择 DP 或 CKKS，服务端分别聚合并融合结果。正文说明 HE 的密钥串谋风险，也描述服务端解密 HE 组结果。因此有机制混合的研究先例，但尚不能作为所有客户端均满足同一完整训练 DP 界的依据。

可作为架构比较对象，不直接照搬噪声参数或通过增加纯 HE 比例证明相同隐私水平下的效用提升。

## 二 与当前代码的对应

以下是对本仓库的检查判断，不是上述论文对本仓库的结论。

| 检查点 | 当前代码位置 | 判断 |
| --- | --- | --- |
| 实际出域扰动 | fmnist_lenet5_dynamic.py 的 _dp_update_release_parameters 和 _distributed_aggregate_dp_parameters | 已区分逐包与聚合扰动，但敏感度所需依赖条件未闭合 |
| 噪声份额 | 同文件的 aggregate_dp_share_stds 循环 | 份额加权方差可数值验证，参与方串谋和掉线条件未实现为完整协议 |
| 边缘模型共享 | 同文件 multi_edge_groups 循环及 shared_returned_state 写入 client_model_states | 已确认共享状态后还有本地训练的执行路径，需要追踪一个客户端数据对后续多个更新的影响 |
| 选择器估计 | selection.py 的 _cached_local_omega_components | 仍用事件计数和旧噪声代价，不能视为已与新聚合机制等价 |
| 可行性过滤 | selection.py 的 _stable_cloud_candidate_pool | 用期望参与人数等参数预先过滤 DP，不能将全 HE 结果解释为完整联合搜索必然优于 DP |
| 密钥与随机数 | he_backend.py；fmnist_lenet5_dynamic.py 的 _seal_ckks_runtime 和 _dp_noise_seed | 单进程角色、可复现实验种子，不等于部署时隔离的密钥与私密随机数 |

## 三 最重要的敏感度条件

下面是一轮理想化查询的数学检查，不是本仓库已证明成立的定理。

设固定、公开、非负的归一化权重为 q_i，客户端更新都裁剪到 C。若在固定先前公开记录的条件下，替换客户端 k 的数据只会改变它自己的更新，则加权和的替换敏感度不超过 2C q_k。对全部受保护客户端取最大值，就得到常见的 2C max_i q_i。

如果共享的未保护模型状态使客户端 k 的数据同时影响一组后续更新 J_k，则仅由各更新范数界能够保证的是 2C sum_{i in J_k} q_i，而不能继续只取 q_k。这个界可能较松，具体需要结合实现缩紧。

一个说明条件必要性的反例是两个等权、范数界为 1 的标量更新。若它们通过共享状态同时由 1 变成 -1，聚合值变化为 2；只按单个客户端权重得到的界为 1。这个反例不表示当前实际训练一定达到该最坏变化，而是说明不能仅凭末端裁剪声称原敏感度成立。

HE 不会消除这种数据依赖。在实际模型更新上核对 J_k，是先于降低噪声和同步选择器的工作。

## 四 推荐方案及其边界

### 保留的主线

继续研究计算位置、聚合结构和隐私保护路径的联合决策，不把已有 DP 会计或 HE 加法本身包装成核心创新。

### 推荐先验证的两条路径

逐包 DP 路径在受信域出口独立保护每个可见更新包。这里不统一称为本地 DP，因为受信边缘可能已经处理多个客户端的贡献。

HE 与聚合 DP 路径只允许向规定的观察者发布含足够噪声的聚合结果。密文、解密请求、份额与噪声种子的可见性均属于协议条件，不能让同一个不受信方得到全部解密能力或可消除的噪声。

两条路径的完整可见记录都要满足同一个选定 DP 目标，才能比较其代价。增加了 HE 并不自动改善精度，是否降低有效噪声取决于公开结果与敏感度分析。

### 不自行改变的事项

- 不从客户端级改为样本级来获得更好的曲线。
- 不因为 Feature DP 效用差就新增受信边缘假设。当前已采用该假设，仍需真实场景支持，且所有模式一致。
- 不删除多次边缘聚合或重置所有模型来简化证明，除非明确接受训练语义改变。
- 不把 pure HE 与完整 DP 方法算作相同保证下的比较对象。
- 不把只有部分原文依据的复现标为原论文实现。
- 不改变预算、学习率、裁剪阈值、基线或既有结果文件。

## 五 后续工作清单

1. 对七种模式画出模型状态的来源及接收方，标记跨轮、跨客户端的未保护状态传播。
2. 在实际参与集合及权重下重新核对敏感度。若改变协作结构或保护粒度才能闭合，需要用户确认。
3. 给每个可见发布建立会计事件，不把通信次数与隐私发布次数机械等同。
4. 用统一噪声计算结果校验选择器和实际训练，但不把噪声能量直接等同于真实收敛误差。
5. 固定数据、拓扑和初始化做短程机制对照，再开展动态路径实验。诊断统计不属于已证明受 DP 保护的公开输出。
6. 用真实 HE 记录加密、聚合、解密与总耗时；profiled 模式仅作数值和开销模型实验。

本轮只形成核对结论，不启动正式训练，不修改 TeX 的核心保护目标。
