# 安全聚合发布的敏感度核对

核对日期为 2026 年 9 月 10 日。当前不改变训练噪声、保护粒度或 TeX 的正式结论。

## 代码事实

`dynfed/fmnist_lenet5_dynamic.py` 中的 `multi_edge_groups` 路径会聚合组内客户端模型，将 `shared_returned_state` 写入组内各客户端的 `client_model_states`，然后继续本地训练。一个客户端的数据因此可能影响多个后续更新，而不只是它自己的更新。

`_distributed_aggregate_dp_parameters` 的历史校准使用归一化包权重乘包内客户端比例。这个乘积需要独立贡献等条件，不能直接用于整包裁剪且存在共享状态传播的所有模式。v29 的整包校准未在本次改动中切回旧公式。

真实 SEAL 路径构造并持有解密器，工作进程可加载私钥。它验证了 CKKS 运算，不构成云端与解密方的权限隔离。只在代码里调用密文求和再解密，不等于云端没有单包解密能力。

## 条件性敏感度界

假定发布量为若干完整裁剪包的固定非负加权和，权重和为一，每包范数不超过 C。给定先前公开记录，令 J_k 包含客户端 k 的数据变化可能影响的全部包，则可由三角不等式得到替换敏感度上界

\[
\Delta \le 2C\max_k\sum_{j\in J_k}q_j.
\]

这要求依赖集合完整，权重和参与集合在所分析的相邻输入之间固定。数据相关的筛选、共享私有历史和额外公开日志均需另行分析。

若每个客户端只影响自己的包，可得到 2C max(q)。若只影响一个边缘包，应使用该边缘包权重，不能再无条件除以边缘内客户端数。若影响全部包，仅凭这些范数界只能保证 2C。

`dynfed/protection_rules.py` 新增的 `aggregate_replacement_bound` 是上述条件的独立审计工具，尚未自动推断训练中的依赖集合，也未接管噪声校准。测试含共享状态反例、边缘分组、独立包及非法权重检查，相关 20 项测试通过。

## 下一阶段

应先做独立的单轮机制对照。固定公开初始模型、参与集合和权重，各客户端独立训练并裁剪更新，比较逐包 DP 与仅发布聚合结果的 DP。后者的噪声必须在非受信观察者取得结果前加入，部署还需要密钥隔离、聚合请求限制及串谋假设。

该对照可以检验聚合发布在其假设下的噪声收益，但不能作为七种协作模式的现成安全证明。多轮共享边缘状态的依赖追踪、公开策略决策的隐私，以及发布协议的隔离仍未完成。暂不启动正式长实验。
