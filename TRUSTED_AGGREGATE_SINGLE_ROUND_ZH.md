# 受信聚合方单轮对照

实验日期为 2026 年 9 月 11 日。

## 目的和边界

用户选择允许受信聚合方看到未加噪聚合结果，在发布前加入 DP 噪声。本实验先检验该发布机制的效用上限，不实现安全传输或 HE。单进程实验程序内部可见所有更新，不能作为单个更新保密的部署实现。此选择不等于授权不受信云端读取各客户端明文。

采用客户端替换邻接、固定名单、固定等权重和公开初始模型，每个客户端独立训练，没有共享私有边缘状态。随机均匀分配 CIFAR 样本用于隔离机制问题，不是正式实验的极端非独立同分布划分。

20 个客户端，1200 个训练样本，200 个测试样本，预训练 ResNet18，3 个本地 epoch，学习率 0.01，GPU 执行。保留总预算 8、delta 为 1e-5、100 次发布的校准时长和裁剪阈值 C=1。本次实际只训练一轮。

同一批客户端更新用于全部对照。DP 路径重复 3 次噪声抽样，使用配对标准正态样本隔离尺度影响。这不是 3 个独立训练种子。逐包 DP 平均噪声直接按其精确高斯分布抽样，不实际传输各噪声包，不据此比较通信或加密耗时。

## 结果

| 路径 | 测试精度 | 发布向量每坐标噪声标准差 |
| --- | --- | --- |
| 无保护 | 30.5% | 0 |
| 仅裁剪 | 30.5% | 0 |
| 逐客户端 DP 后求平均 | 7.5%、12.5%、8.0% | 3.0908 |
| 受信聚合方求平均后加 DP | 8.5%、11.5%、8.5% | 0.6911 |

初始精度为 5.5%。本轮没有客户端触发贡献裁剪。有效平均更新范数约 0.1511。逐包 DP 平均后的噪声范数约 10013，聚合后 DP 噪声范数约 2239，后者仍是信号范数的约 14818 倍。

独立等权贡献条件下，逐包敏感度为 2C，聚合敏感度为 2C/N。在同一噪声乘子下，两种发布噪声标准差相差 sqrt(N)，本例约 4.47 倍。降低噪声是通过改变接收方可见信息和信任条件取得的，不是同一观察视图下无代价的改进。

## 结论

这次效用验证未通过。调整加噪位置确实降低了噪声，但当前约 1049 万个可训练参数、20 个客户端和全程预算 8 的组合仍造成严重失真。本实验不能说明所有客户端级 DP 都无法训练，也不能证明换成 100 个客户端就必然成功。

下一步应先做小规模、预先固定的裁剪阈值对照，观察裁剪偏差和噪声共同影响；必要时再单独研究固定低维训练子空间及参与规模。不根据私有范数自适应调整公开阈值而忽略该操作的隐私代价，不直接扩大预算或运行长实验。

本报告及全部无保护指标、重复试验结果只用于内部诊断。若联合公开这些结果，需要另行分析隐私消耗。确定性实验随机种子不作为生产 DP 随机源。多轮模型、动态策略、共享状态与密钥隔离的安全分析仍未完成。

原始记录位于 `out/trusted_aggregate_single_round/2026-09-11_22-36-51_trusted_aggregate/report.json`。训练数据和数值未修改；报告的信任字段在运行后澄清为受信方授权读取聚合结果，并单列单进程模拟可见更新的限制。

## 固定裁剪阈值对照结果

后续复用同一批独立训练的客户端更新，测试 C 为 1、0.1、0.01、0.001、0.0001、0.00001。模型、预算、校准时长和样本不变。每个 DP 设置进行 3 次配对噪声抽样，不是多训练种子，也不是逐轮训练结果。

| C | 仅裁剪精度 | 逐客户端 DP 后平均精度均值 | 聚合后 DP 精度均值 |
| --- | --- | --- | --- |
| 1 | 30.5% | 9.33% | 9.50% |
| 0.1 | 13.5% | 9.33% | 8.67% |
| 0.01 | 6.5% | 10.83% | 11.00% |
| 0.001 | 6.0% | 7.33% | 5.67% |
| 0.0001 | 5.5% | 5.67% | 5.50% |
| 0.00001 | 5.5% | 5.50% | 5.50% |

未发现恢复有效单轮学习的阈值。不能把 11% 相对于其他噪声设置的提高视为收敛证据，也不能把 5.5% 的稳定误认为改善。无保护为 30.5%，初始精度为 5.5%。

当 C 不超过 0.1 时，本例全部贡献被裁剪。对同一组非零更新，裁剪后的平均信号随 C 线性缩放；校准噪声也随 C 线性缩放。因此继续缩小 C 不能改善相应的噪声信号比。聚合后 DP 的范数比稳定在约 3765，逐包 DP 后平均约 16839。这个结论针对本轮全部裁剪的情况，不意味着不同训练轨迹下的所有阈值都等价。

本次没有修改正式阈值，没有选择并发布一个宣称满足 DP 的最佳模型。调参记录只作内部诊断，数据相关参数选择若进入正式发布仍需分析隐私代价。下一步应优先验证正式规模的 100 客户端单轮条件，避免将 20 客户端诊断直接等同于正式实验。若仍失败，再讨论低维训练或预算变更，不直接启动长实验。

原始记录位于 `out/trusted_aggregate_clip_sweep/2026-09-11_23-01-41_trusted_aggregate/report.json`。脚本新增 `--clip-norms`，不为每个阈值重复本地训练。

## 100 客户端对照与有限细查

同日将客户端数改为 100，训练样本增加至 6000，以保持每客户端 60 个样本。其余配置不变，测试仍为 200 个样本。此实验不是固定总数据量的纯规模对照，也不是正式的 12000 样本和极端非独立同分布实验。仍为固定等权独立训练的单轮机制诊断。

先运行原来的 6 个阈值，再根据粗扫描结果进行一次 0.03、0.05、0.1、0.15、0.2、0.3 的探索性细查。细查复用相同种子与数据分配重建本地更新，每次调用内部各阈值共享更新。阈值选择不是事先完整注册的，因此结果只作内部探索，不作为独立测试集上的最终性能估计。

| C | 仅裁剪精度 | 聚合后 DP 精度均值 | 聚合后 DP 损失均值 |
| --- | --- | --- | --- |
| 0.03 | 7.5% | 10.83% | 2.3740 |
| 0.05 | 9.0% | 13.00% | 2.3528 |
| 0.1 | 13.0% | 14.33% | 2.4383 |
| 0.15 | 21.0% | 13.33% | 2.8710 |
| 0.2 | 28.0% | 11.17% | 3.9781 |
| 0.3 | 32.5% | 12.17% | 11.2001 |

初始精度 5.5%，初始损失 2.4462；无保护精度 32.5%，损失 2.0073。DP 数值仍为同一轮更新上的 3 次配对噪声抽样均值，不是独立训练种子的统计量。

在已测设置中聚合后 DP 的最高平均精度为 14.33%，有单轮改善，但未接近无保护效用；它也不是损失最小的设置。不能据此断言最终不能收敛，不能仅按最高精度选正式参数。原先 20 客户端的不足没有因扩大至 100 客户端而完全消失。

固定 C 时，100 客户端的聚合噪声标准差为 20 客户端的五分之一。C=0.1 时为约 0.01382，而 1049 万维的噪声范数仍约 44.78，裁剪后平均更新范数约 0.05643。这个比例不是收敛定理，但与观察到的效用损伤一致。

本次没有改正式训练配置、预算、学习率或论文。后续若验证冻结骨干只训练分类头，应明确这是单独的训练子空间消融，不能偷偷替换全模型基线。当前规模仍不足以得出所有客户端级 DP 都不可用的结论。

粗扫描原始记录为 `out/trusted_aggregate_100_clients/2026-09-11_23-10-20_trusted_aggregate/report.json`。

细查原始记录为 `out/trusted_aggregate_100_clients_refine/2026-09-11_23-11-26_trusted_aggregate/report.json`。

## 分类头低维对照

用户选择 A 后运行分类头诊断。100 客户端、6000 训练样本、200 测试样本、同一数据分配种子、3 个本地 epoch、学习率 0.01、总预算 8、100 次发布校准时长均不变。仍为独立单轮与 3 次配对噪声抽样。

训练范围需要澄清。原来的 `resnet18_pretrained` 更新 layer3、layer4 和分类头，约 1049 万个参数，并不是整个 ResNet18 均可训练；`resnet18_pretrained_head` 只更新 5130 个分类头参数。两者均冻结 BatchNorm，低维实验没有给冻结参数加噪。

| C | 分类头仅裁剪精度 | 分类头聚合后 DP 精度均值 | 分类头聚合后 DP 损失均值 |
| --- | --- | --- | --- |
| 0.03 | 7.0% | 7.83% | 2.4102 |
| 0.05 | 8.0% | 8.67% | 2.3913 |
| 0.1 | 10.0% | 10.50% | 2.3628 |
| 0.15 | 11.0% | 12.33% | 2.3609 |
| 0.2 | 12.0% | 13.17% | 2.4061 |
| 0.3 | 13.0% | 12.50% | 2.6014 |

初始精度仍为 5.5%，初始损失 2.4462。分类头无保护精度为 13.0%，损失为 2.2331。因此不能拿分类头 DP 与此前约 1049 万参数训练的无保护精度 32.5% 直接比较。C=0.2 的 DP 平均精度 13.17% 与同结构无保护精度接近，但 DP 损失明显更高，单轮和 200 测试样本不足以说明效用等价。

在 C=0.1 时，分类头聚合后噪声与更新范数之比约 15.51，之前较大训练范围下约 793.57。减少维度明显减轻了向量扰动，却同时减少了可训练能力。当前只能认为数值破坏减轻，不能认定长期训练通过。

逐包 DP 平均在 C=0.1 时损失约 5.2331，聚合后 DP 为 2.3628。受信聚合带来的效用优势在这个对照中仍可见，但不代表无信任条件也获得了相同保证。

下一步可以预先固定 C=0.1 与 C=0.15，做相同分类头结构下的短程多轮轨迹对照，保留无保护与仅裁剪对照，并保持 100 次发布的预算校准。参数只作为后续验证候选，尚未改入正式配置。正式参数确定还需要独立验证数据和训练种子；不能把本次探索的最高精度当成最终结果。

本次没有修改正式模型、正式噪声或 TeX，没有使用 HE，没有证明七种模式的完整 DP。原始记录为 `out/trusted_aggregate_100_clients_head/2026-09-11_23-16-37_trusted_aggregate/report.json`。

## 分类头五轮轨迹对照

2026 年 9 月 12 日新增 `experiments/validate_trusted_aggregate_trajectory.py`，运行 100 客户端、6000 训练样本、200 测试样本、固定随机分配和种子 42 的五轮对照。每个客户端每轮从本路径上轮发布模型开始，独立本地训练 3 个 epoch，随后等权聚合。优化器按现有本地训练函数每次新建，不跨客户端复用动量。没有跨路径共用后续更新。

预先固定本阶段 C=0.1 和 C=0.15，分类头 5130 维，学习率 0.01。噪声仍按 100 次发布、epsilon=8、delta=1e-5 校准，乘子约 6.9112，没有按实际五轮降低噪声。每个设置只运行一条训练和噪声轨迹；不同阈值使用配对标准正态随机数，每轮使用不同种子，不能将它们视为独立重复。

| 路径 | 第1轮 | 第2轮 | 第3轮 | 第4轮 | 第5轮 | 第5轮损失 |
| --- | --- | --- | --- | --- | --- | --- |
| 无保护分类头 | 13.0% | 22.5% | 29.0% | 32.5% | 39.0% | 1.8405 |
| 仅裁剪 C=0.1 | 10.0% | 14.5% | 21.5% | 27.0% | 29.5% | 1.9850 |
| 聚合后 DP C=0.1 | 11.0% | 11.5% | 18.5% | 20.0% | 22.0% | 2.1015 |
| 仅裁剪 C=0.15 | 11.0% | 21.0% | 29.0% | 32.0% | 36.5% | 1.8606 |
| 聚合后 DP C=0.15 | 12.0% | 13.5% | 19.5% | 22.0% | 22.0% | 2.0871 |

两条 DP 路径的损失均逐轮下降。C=0.1 从 2.3623 降至 2.1015，C=0.15 从 2.3618 降至 2.0871。当前五轮没有出现数值崩塌，并有持续的学习信号；这不等于证明长期收敛或效用已足够。DP 与相同阈值仅裁剪对照之间仍有明显差距，不能仅通过放宽裁剪解决。

每条 DP 路径记录五次聚合发布，其条件性事件记账 epsilon 均约 1.605。该值没有包含其他实验路径、未保护对照及私有诊断指标的联合发布，不是整个实验公开后的隐私保证。没有保护的对照记录 epsilon 为 null，避免把不记账误读为零隐私泄露。

本实验仍是受信聚合方的单进程机制诊断，不实现 HE 或单个更新保密传输，未接回动态选择器和七种模式。脚本只保存内部逐轮结果，不支持检查点续跑；更长对照需要从相同种子重新开始或先单独实现完整状态恢复，不拼接不相容结果。

15 项相关测试通过，覆盖更新累计、初始与冻结状态不被修改、非法发布拒绝、100 次发布校准保留及单轮尺度校验。正式配置和 TeX 未修改。

下一步可做同口径 20 轮验证，仍保留两个阈值和对应对照，之后才考虑多种子、正式数据分配及 HE 协议验证。不根据五轮结果宣称 100 轮不会退化。

结果位于 `out/trusted_aggregate_head_trajectory/2026-09-12_08-44-18_trajectory/report.json`。

## 分类头二十轮轨迹复核

2026 年 9 月 12 日延长至 20 轮，仍按 100 次发布、总预算 8 校准。因为诊断脚本无续跑功能，从相同种子重新运行全部五条路径，逐项比对前五轮的全部记录，25 条均完全一致。五条路径共 100 条记录，状态为 completed，记录的训练和评估总时间约 340.8 秒，不含前置数据与模型加载。

| 路径 | 第20轮精度 | 最高精度 | 最后5轮平均精度 | 第20轮损失 |
| --- | --- | --- | --- | --- |
| 无保护分类头 | 46.0% | 46.5% | 46.2% | 1.4612 |
| 仅裁剪 C=0.1 | 46.0% | 46.0% | 45.1% | 1.5350 |
| 聚合后 DP C=0.1 | 40.0% | 41.0% | 39.7% | 1.7787 |
| 仅裁剪 C=0.15 | 46.0% | 46.5% | 46.2% | 1.4650 |
| 聚合后 DP C=0.15 | 36.0% | 38.0% | 36.9% | 1.9262 |

最后五轮平均是同一训练轨迹的时间平均，不是独立种子均值。每条 DP 路径记账 20 次，条件性事件预算约 3.3146。其他数据和信任条件仍与五轮对照相同，不将此值解释为全部对照联合公开的 DP 保证。

两条 DP 路径没有退回随机水平或出现此前巨大损失，但并非单调收敛。C=0.15 在第 18 轮达到 38%，第 20 轮回落至 36%；末段损失反复，最终也高于 C=0.1。C=0.1 最终精度仍比无保护低 6 个百分点。当前支持低维受信聚合 DP 可以在该配置下持续学习，不支持长期不会退化或已解决正式方法效用的结论。

下一步建议先在其他种子上复核这两个固定阈值，再决定是否延长至 100 轮，避免仅凭调参种子 42 选出最终方案。正式非独立同分布划分、动态模式、更新传输保密与 HE 协议仍未接入本诊断，正式配置与 TeX 未改。

原始结果为 `out/trusted_aggregate_head_20rounds/2026-09-12_08-47-46_trajectory/report.json`。

## 三个种子的探索性复核

2026 年 9 月 12 日补跑 seed=40 和 44，同样运行五条路径各 20 轮，不改模型、预算、阈值、数据量、客户端规模或本地训练参数。两份新报告均为 completed，各含 100 条逐轮记录。

| 第20轮精度 | seed=40 | seed=42 | seed=44 | 描述性均值 |
| --- | --- | --- | --- | --- |
| 无保护 | 55.5% | 46.0% | 53.5% | 51.67% |
| 仅裁剪 C=0.1 | 54.5% | 46.0% | 53.5% | 51.33% |
| 聚合后 DP C=0.1 | 39.0% | 40.0% | 35.5% | 38.17% |
| 仅裁剪 C=0.15 | 56.0% | 46.0% | 54.0% | 52.00% |
| 聚合后 DP C=0.15 | 35.5% | 36.0% | 35.0% | 35.50% |

本批三条 C=0.1 的最终精度均高于各自 C=0.15，但相对于无保护的平均差距仍约 13.5 个百分点，不能称为效用问题已解决。各路径存在中途反复；目前支持在这些诊断条件下可学习，不支持长期不会退化、收敛证明或正式混合隐私框架已经通过。

### 随机流限制和修复

运行中检查发现旧噪声种子采用 `seed + 10000 + round_idx`。例如 seed=40 的第3轮与 seed=42 的第1轮具有同一噪声种子，因此不同种子的轨迹间存在跨轮噪声复用。以上结果保留为探索性描述，不能当作完全独立的重复实验来计算置信区间或宣称显著性。

脚本已将后续新运行默认改成 `SeedSequence([seed, round_idx, 10000])` 派生的随机流，日志新增 `noise_stream`。同一实验种子、同一轮的不同阈值仍保持配对，这与跨实验种子碰撞不同。新增测试验证旧碰撞反例、新方案在 3 个种子和 100 轮的 300 个组合中不重复、确定性复现与非法参数拒绝。共 19 项轨迹和单轮诊断测试通过。有限组合不碰撞的测试不等于生产随机性或 DP 安全证明，随机数仍用于可复现实验。

本批两个进程都在修复前已经启动，因此均按旧规则执行，没有混入新规则结果。已经生成的数据未修改。原先没有 `noise_stream` 字段的轨迹报告对应旧规则；重现时可传入 `--noise-stream legacy_additive`。新默认随机流下的前五轮数值不应期待与旧流完全相同。

下一步优先用新随机流复核固定阈值，再决定是否延长训练。新流完整三种子实验尚未运行，不将本批直接写为论文中的最终统计结果。原有正式训练配置与 TeX 未修改。

新增原始记录分别位于 `out/trusted_aggregate_head_20rounds_seed40/2026-09-12_08-55-37_trajectory/report.json` 和 `out/trusted_aggregate_head_20rounds_seed44/2026-09-12_09-01-32_trajectory/report.json`。

## 修正随机流后的 100 轮验证

2026 年 9 月 12 日运行 seed=42，显式采用 `--noise-stream seed_sequence`，五条路径各 100 轮。两个裁剪阈值、分类头模型、学习率、100 客户端、6000 训练样本、200 测试样本和等权随机分配均不变。噪声从始至终按 100 次发布的总预算 8 校准，没有调整后期预算或临时切换机制。

报告状态 completed，共 500 条记录。训练与评估总耗时约 1385.4 秒，即 23.1 分钟，不含脚本前置加载阶段。这是五条串行诊断路径的总时间，不是动态方法的端到端部署时延。

| 路径 | 最终精度 | 最高精度 | 最后10轮平均精度 | 最终损失 |
| --- | --- | --- | --- | --- |
| 无保护 | 60.0% | 60.0% | 59.85% | 1.2013 |
| 仅裁剪 C=0.1 | 59.5% | 59.5% | 58.20% | 1.2176 |
| 聚合后 DP C=0.1 | 42.0% | 48.0% | 43.30% | 1.7940 |
| 仅裁剪 C=0.15 | 60.0% | 60.0% | 59.80% | 1.2017 |
| 聚合后 DP C=0.15 | 41.0% | 44.5% | 40.90% | 2.2661 |

两条 DP 路径均在第 62 轮取得本次最高精度。C=0.1 最终较峰值回落 6 个百分点，最低损失约 1.6163，最终回升至 1.7940。C=0.15 最终较峰值回落 3.5 个百分点，最低损失约 1.8533，最终升至 2.2661。损失反弹不能因为精度仍高于随机水平而忽略。

### 如何解释

当前结果不是早期接近随机猜测的严重失效，支持在既定受信聚合和低维训练条件下持续学习。但它也不支持单调或稳定收敛的结论，后期存在精度波动和效用退化。最终对无保护的精度差距分别为 18 和 19 个百分点。隐私保护可以产生效用代价，这个差距本身不是机制不合理的充分证据，仍需与同等保护要求的方法比较。

C=0.1 的平均裁剪更新范数从第一轮约 0.06383 降至第100轮约 0.03404，而每轮噪声范数一直约为 1。C=0.15 对应更新范数从约 0.09573 降至约 0.05223，噪声范数约为 1.5。固定噪声相对于有效更新的影响增大，与观察到的后期波动相符，但单条轨迹不能证明所有变化均由这一因素导致。

两条 DP 路径记录 100 次发布，最终事件记账 epsilon 约为 8。波动不是预算到期后切为 HE 引起的，本实验从未使用 HE。最终达到预算也不表示能够继续追加新的非零消耗发布而不重新校准或调整发布计划。

采用新随机流后，DP 的前20轮不应与旧 additive 流结果直接拼接。当前是新流单种子 100 轮诊断，仍不是独立多种子的最终论文统计。报告和未保护对照只用于内部诊断，正式协议的密钥隔离、动态策略、共享状态和所有公开输出的分析仍未完成。

下一阶段应研究同一隐私目标下的效用和稳定性权衡，而不是以追平无保护为通过标准。可单独比较预先固定的服务器步长衰减或发布频率设置，但这些会改变训练或发布计划，不能未经说明纳入正式实验。当前未修改这些参数，也未改正式配置和 TeX。

原始结果为 `out/trusted_aggregate_head_100rounds_seed42/2026-09-12_09-26-24_trajectory/report.json`。

## 固定服务器步长对照

2026 年 9 月 12 日固定 C=0.1、seed=42、新随机流和原有分类头训练配置，运行服务器步长 1、0.5、0.2，各保留无保护、仅裁剪、聚合后 DP 三条路径，共 180 条逐轮记录。每条路径独立从初始模型训练 20 轮，仍按 100 次发布校准总预算 8。运行完成，训练与评估总耗时约 385.3 秒。

服务器执行 `w_next = w + server_step * noisy_update`，不是只缩小噪声。新增日志分别记录原始信号、原始噪声、实际应用信号与实际应用噪声。候选步长事先固定，与私有数据无关；原机制的事件记账不因固定正数缩放而减少。此处只讨论当前条件性发布机制，不扩大为完整协议的 DP 证明。

| 服务器步长 | 无保护精度 | 仅裁剪精度 | DP精度 | DP损失 |
| --- | --- | --- | --- | --- |
| 1 | 46.0% | 46.0% | 35.5% | 1.7697 |
| 0.5 | 43.5% | 42.0% | 35.0% | 1.8363 |
| 0.2 | 32.0% | 26.5% | 26.0% | 2.0753 |

步长 1 的三条路径共 60 条精度与损失记录，与之前新流 100 轮报告的相应前缀完全一致。三条 DP 路径最终事件记账均为约 3.3146，并非小步长消耗更少预算。

全程固定小步长在本次 20 轮下未改善最终精度或损失。步长 0.2 缩小了 DP 与仅裁剪的差距，却同时降低两者的学习进度，不能将其认定为稳定性问题已解决。该实验没有覆盖此前第60轮之后的退化区间，不能据此断言小步长对后期完全无益。

脚本新增 `--server-steps`，默认仍为 1。27 项相关测试通过，覆盖完整加噪更新缩放、输入边界及原有裁剪和记账功能。没有修改正式训练配置和 TeX。

下一阶段存在需要选择的路线。保持当前方法并延长小步长对照，可检验时间尺度效应；改成预先固定的后期步长衰减，会改变训练计划，需要明确衰减起点和规律。不能仅因为本次结果不理想就不断新增未经计划的调参，也不能把诊断集上选出的方案直接当最终论文结果。

原始记录位于 `out/trusted_aggregate_head_server_steps/2026-09-12_13-57-04_trajectory/report.json`。
