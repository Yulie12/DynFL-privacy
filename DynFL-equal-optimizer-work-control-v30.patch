diff --git a/dynfed/fmnist_lenet5_dynamic.py b/dynfed/fmnist_lenet5_dynamic.py
index a47a654..ad406d7 100644
--- a/dynfed/fmnist_lenet5_dynamic.py
+++ b/dynfed/fmnist_lenet5_dynamic.py
@@ -102,6 +102,7 @@ class Lenet5Config:
     update_parameter_scope: str = CURRENT_UPDATE_PARAMETER_SCOPE
     execution_revision: str = CURRENT_EXECUTION_REVISION
     local_epochs: int = 1
+    equal_optimizer_work_control: bool = False
     learning_rate: float = 0.15
     l2: float = 0.0001
     iid: bool = False
@@ -1063,6 +1064,11 @@ def _run_lenet5_policy(
                 f"requested={train_config.execution_revision!r}. Start a new run so "
                 "aggregation semantics do not change mid-training."
             )
+        if bool(saved_training.get("equal_optimizer_work_control", False)) != bool(train_config.equal_optimizer_work_control):
+            raise RuntimeError(
+                "Cannot resume across different equal-optimizer-work control settings. "
+                "Start a new run so optimizer-work semantics do not change mid-training."
+            )
         saved_he_execution = saved_training.get("he_execution", "real")
         if saved_he_execution != train_config.he_execution:
             raise RuntimeError(
@@ -3095,6 +3101,7 @@ def _run_lenet5_policy(
     )
     summary["global_pareto_selection_rounds"] = global_pareto_selection_rounds
     summary["execution_revision"] = train_config.execution_revision
+    summary["equal_optimizer_work_control"] = bool(train_config.equal_optimizer_work_control)
     summary["dp_accounting_mode"] = privacy_parameters["accounting_mode"]
     summary["dp_delta"] = privacy_parameters["delta"]
     summary["dp_feature_epsilon_target"] = privacy_parameters["feature_budget"]
@@ -3253,6 +3260,10 @@ def _client_step_limit(selection):
 
 def _client_epoch_count(train_config, selection, candidate):
     stages = max(1, int(MODE_SPECS[candidate.mode].E_edge_loops)) if selection.mainline_fusion else 1
+    # Fairness control only: equalize optimizer work across collaboration modes without
+    # changing the main Method-2 execution semantics used by formal runs.
+    if selection.mainline_fusion and train_config.equal_optimizer_work_control:
+        return int(train_config.local_epochs)
     # Continuous local optimizer state, private to this client, across stages.
     return int(train_config.local_epochs) * stages
 
diff --git a/experiments/run_fmnist_lenet5.py b/experiments/run_fmnist_lenet5.py
index e1b5331..1ba606b 100644
--- a/experiments/run_fmnist_lenet5.py
+++ b/experiments/run_fmnist_lenet5.py
@@ -62,6 +62,15 @@ def parse_args() -> argparse.Namespace:
     parser.add_argument("--dataset", default="fmnist", choices=["fmnist", "cifar10", "cifar100"])
     parser.add_argument("--data-root", default=None)
     parser.add_argument("--local-epochs", type=int, default=1)
+    parser.add_argument(
+        "--equal-optimizer-work-control",
+        action="store_true",
+        help=(
+            "Fairness-control only: in fused Method 2, keep total optimizer epochs per "
+            "client equal to --local-epochs for every collaboration mode instead of "
+            "multiplying by the mode's private-stage count."
+        ),
+    )
     parser.add_argument(
         "--model",
         default="lenet5",
@@ -280,6 +289,7 @@ def main() -> None:
         dataset_name=args.dataset,
         model_name=args.model,
         local_epochs=args.local_epochs,
+        equal_optimizer_work_control=args.equal_optimizer_work_control,
         learning_rate=args.lr,
         iid=args.iid,
         partition_mode="iid" if args.iid else args.partition_mode,
diff --git a/experiments/run_paper_config.py b/experiments/run_paper_config.py
index d1813e3..7364693 100644
--- a/experiments/run_paper_config.py
+++ b/experiments/run_paper_config.py
@@ -31,6 +31,11 @@ def parse_args() -> argparse.Namespace:
     parser.add_argument("--rounds", type=int)
     parser.add_argument("--he-execution", choices=["real", "profiled"])
     parser.add_argument("--output-root")
+    parser.add_argument(
+        "--equal-optimizer-work-control",
+        action="store_true",
+        help="Run the supplementary equal-optimizer-work fairness control; main formal runs leave this off.",
+    )
     parser.add_argument(
         "--max-new-rounds",
         type=int,
@@ -64,6 +69,7 @@ def build_command(
     rounds: int | None,
     max_new_rounds: int | None = None,
     resume_from_run: Path | None = None,
+    equal_optimizer_work_control: bool = False,
 ) -> list[str]:
     if config.get("execution_protocol") == method2_config.PROTOCOL:
         return method2_config.build(config, ROOT, seed=seed, policies=policies, rounds=rounds,
@@ -138,6 +144,8 @@ def build_command(
     for name, value in values.items():
         _append_value(command, name, value)
     command.extend(["--update-mechanisms", *privacy.get("candidate_mechanisms", ["dp", "he3", "dp_he3"])])
+    if equal_optimizer_work_control:
+        command.append("--equal-optimizer-work-control")
     if max_new_rounds is not None:
         _append_value(command, "max_new_rounds", max_new_rounds)
     if resume_from_run is not None:
@@ -187,6 +195,7 @@ def main() -> None:
             rounds=args.rounds,
             max_new_rounds=args.max_new_rounds,
             resume_from_run=args.resume_from_run,
+            equal_optimizer_work_control=args.equal_optimizer_work_control,
         )
         print(subprocess.list2cmdline(command), flush=True)
         if not args.dry_run:
diff --git a/tests/test_fused_he_release.py b/tests/test_fused_he_release.py
index d13c81d..57be1a8 100644
--- a/tests/test_fused_he_release.py
+++ b/tests/test_fused_he_release.py
@@ -40,6 +40,13 @@ def test_formal_method2_command_preserves_tested_scope_and_horizon():
     assert cmd[cmd.index("--rounds") + 1] == "100"
     assert cmd[cmd.index("--local-epochs") + 1] == "3"
     assert cmd[cmd.index("--he-execution") + 1] == "real"
+    assert "--equal-optimizer-work-control" not in cmd
+
+    control_cmd = build_command(
+        config, seed=42, policies=["ours"], rounds=None,
+        equal_optimizer_work_control=True,
+    )
+    assert "--equal-optimizer-work-control" in control_cmd
 
 
 def test_multilevel_stages_extend_only_the_clients_own_training():
@@ -53,6 +60,16 @@ def test_multilevel_stages_extend_only_the_clients_own_training():
     assert _client_epoch_count(training, selection, SimpleNamespace(mode="LIIC")) == 3
 
 
+def test_equal_optimizer_work_control_does_not_change_mainline_default():
+    from dynfed.fmnist_lenet5_dynamic import _client_epoch_count, Lenet5Config
+    from dynfed.selection import SelectionConfig
+    from types import SimpleNamespace
+    selection = SelectionConfig(mainline_fusion=True)
+    control = Lenet5Config(local_epochs=3, equal_optimizer_work_control=True)
+    for mode in ("LIIC", "LIEIIIC", "LIIEIIIC"):
+        assert _client_epoch_count(control, selection, SimpleNamespace(mode=mode)) == 3
+
+
 def test_all_fused_modes_reach_one_release_with_no_repeated_edge_aggregation():
     import random
     from dynfed.selection import SelectionConfig, enumerate_candidates, _profile_flow_result
diff --git a/tex/paper/main.tex b/tex/paper/main.tex
index a0f8e8e..92e92f4 100644
--- a/tex/paper/main.tex
+++ b/tex/paper/main.tex
@@ -409,6 +409,10 @@ use one stage of three epochs. A client continues its own model and optimizer
 state across stages, starting from the previous released global model. There is
 no same-round cross-client aggregate feedback. Clipping and distributed Gaussian
 noise are applied once to the final client contribution, after all its stages.
+As a supplementary fairness control, we also provide an equal-optimizer-work setting
+that caps every collaboration mode at the same total local-epoch budget. This control
+is disabled for the main Method-2 experiments and is reported separately; it tests
+whether gains persist when the additional private-stage optimizer work is removed.
 Every executable mode, including LIE and LIIE, then joins the same release overlay:
 one trusted noisy packet per edge domain, ciphertext-only cloud aggregation,
 authorized custodian decryption, and global update return. The basic templates'
@@ -894,8 +898,7 @@ Here \(\prec\) denotes Pareto dominance. The latency range is
 \label{eq:pareto_normalization}
 \end{equation}
 Here \(\varepsilon_n>0\) prevents division by zero. The reference point
-\(\mathbf z^{r,t}=[z_T^{r,t},z_\Omega^{r,t}]^\top\) can be prescribed or set
-to the computed ideal point
+\(\mathbf z^{r,t}=[z_T^{r,t},z_\Omega^{r,t}]^\top\) is set to the computed ideal point
 \([T_{\min}^t,\widehat\Omega_{\min}^t]^\top\). Its normalized form is
 \(\bar{\mathbf z}^{r,t}=[\bar z_T^{r,t},\bar z_\Omega^{r,t}]^\top\).
 Using the normalized Tchebycheff distance to the reference or ideal
@@ -974,8 +977,9 @@ by changing collaborative modes is included in \(T_i^t\).
 The fused mainline does not apply a DP-versus-HE substitution safeguard.
 Result DP is a mandatory release constraint and HE is the fixed confidentiality
 layer for the cloud aggregation path, so neither can be selected as a replacement
-for the other. Before the bounded search, the implementation checks that one
-additional release remains feasible under the fixed RDP horizon. The common
+for the other. The fixed release accountant is configured for the prospective
+round before selection, while its authoritative cohort and budget check is performed
+immediately before the global release. The common
 release-noise term is evaluated from the actual public aggregation weights via
 \equref{eq:fused_sensitivity}; because this protection contract is common to all
 feasible modes, it does not create an artificial mode-specific utility preference.
@@ -1009,10 +1013,10 @@ aggregate.
 \label{alg:selection}
 \small
 \begin{algorithmic}[1]
-\REQUIRE Device set \(\mathcal N\), executable resource-feasible modes \(\mathcal H_i^{\mathrm{res}}\subseteq\mathcal H\), fixed global release RDP state \(R_{\mathrm{upd}}^t(\alpha)\), target \((\bar\epsilon_{\mathrm{upd}},\delta_{\mathrm{upd}})\), round system state, path latency and buffer models, \(K_{\mathrm P},K_{\mathrm B},I_{\max},S_P\), optional \(\mathbf z^{r,t}\), and \(\mathbf x^{t-1}\)
+\REQUIRE Device set \(\mathcal N\), executable resource-feasible modes \(\mathcal H_i^{\mathrm{res}}\subseteq\mathcal H\), fixed global release RDP state \(R_{\mathrm{upd}}^t(\alpha)\), target \((\bar\epsilon_{\mathrm{upd}},\delta_{\mathrm{upd}})\), round system state, path latency and buffer models, \(K_{\mathrm P},K_{\mathrm B},I_{\max},S_P\), and \(\mathbf x^{t-1}\)
 \ENSURE Selected global collaboration profile \(\mathbf x^\star=\mathbf h_{\mathcal N}^\star\)
 \STATE \textbf{Preparation}
-\STATE Check that the fixed release accountant permits one additional global release at the calibrated noise multiplier.
+\STATE Verify that the fixed release accountant is configured for the prospective round; the authoritative cohort/budget check is performed immediately before the global release.
 \STATE For every \(i\in\mathcal N\), construct \(\mathcal S_i=\{h_i\mid h_i\in\mathcal H_i^{\mathrm{res}}\}\).
 \STATE \(\displaystyle
 \mathcal X^t=\left\{\mathbf x\in\prod_{i\in\mathcal N}\mathcal S_i
@@ -1063,7 +1067,7 @@ updates using \equref{eq:effective_aggregation_weight} and
 \frac{\widehat\Omega_{\mathrm{sys}}^t(\mathbf x)-
 \widehat\Omega_{\min}^t}
 {\widehat\Omega_{\max}^t-\widehat\Omega_{\min}^t+\varepsilon_n}\).
-\STATE Use the prescribed \(\mathbf z^{r,t}\), or set it to
+\STATE Set \(\mathbf z^{r,t}\) to
 \([T_{\min}^t,\widehat\Omega_{\min}^t]^\top\), and apply the same normalization to obtain
 \((\bar z_T^{r,t},\bar z_\Omega^{r,t})\).
 \STATE \(\displaystyle
