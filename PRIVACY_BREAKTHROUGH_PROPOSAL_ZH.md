# 隐私保证与训练效用的突破方案

本方案根据 2026 年 9 月 8 日的 main.tex、v28 实现及 2024 至 2025 年文献形成。它是待验证的设计方案，不是已经完成的隐私证明或实验结果。本次不修改正式训练配置及 TeX。

## 核对结论

当前包含两个不同问题。一个是保证对象与协议条件尚未闭合，另一个是在既定客户端级预算下对高维更新加噪的效用成本。修正其中一个不自动解决另一个。

TeX 在系统模型中将关联端和边缘设为受信域，将云设为诚实但好奇，并允许出域更新使用 DP 或 HE。在 Privacy Budget Accounting 中，邻接关系是替换一个客户端的完整贡献，客户端或边缘包整体裁剪后采用 2C 的敏感度界。在聚合描述中，受信协调者仅解密最终加权和。

代码的组合路径先裁剪各客户端更新，再平均形成边缘包，并使用包内客户端占比和最终聚合权重降低噪声。这与 TeX 的整包裁剪机制不同。前者需要证明替换某个客户端时哪些后续输出会变化。共享边缘状态后继续训练，可能使一个客户端影响多个贡献。

TeX 的 RDP 增量用机制等于 DP 的指示项。若正式候选含 DP 与 HE 的组合，该项应包含所有实际执行 DP 的机制，并依据真正的可见发布定义事件，不能遗漏组合机制。selection.py 的固定 MECHANISM_RISK 系数可作为建模输入，但不是 DP 界或 HE 安全证明。

## 可复算的量级检查

当前预训练 ResNet 18 的可训练参数数目为 10,490,890。使用现有非采样高斯 RDP 校准器，以 epsilon=8、delta=0.00001、100 次发布计算，噪声倍率为 6.9112。

在固定公开权重、每个客户端仅影响自己的贡献、100 个客户端等权且均参与的理想模型中，裁剪半径为 C。聚合噪声的均方根范数为

\[
\sqrt{\mathbb E\|Z\|_2^2}=2\sigma C\sqrt d/100.
\]

| 实际被扰动的维度 d | 理想聚合噪声的均方根范数 |
| --- | --- |
| 10,490,890 | 447.70 C |
| 5,130 | 9.90 C |
| 100 | 1.38 C |

这些数值来自现有会计器和理想公式，不是当前动态拓扑的隐私证明，也不是精度预测。表中 100 表示实际发布的向量维度，不是 LoRA 的秩。

在同一理想模型中，逐客户端独立扰动再平均的噪声均方根范数约为 4,477.03 C。安全聚合可改善这一量级，但仍留下严重的高维效用压力。不能把此推导称为所有 DP 方法的不可训练定理。

当大量贡献进入裁剪区间时，减小 C 同时缩小信号和噪声。因此降低 C 可以改变优化稳定性，却不自动改变信噪比的主要维度依赖。

## 推荐主线

优先保留客户端级替换邻接，建立同一个发布隐私目标下的两条执行路径，再通过低维训练验证效用。

路径一在受信域出口对完整的发布贡献裁剪、扰动，然后发送给云。对云可见的单独更新包进行会计。

路径二在出域前形成受保护贡献，进行真实 HE 聚合，由隔离的协调者只对经验证的聚合结果解密并添加足够的噪声，然后发布。也可以采用有相应协议证明的分布式噪声生成，但必须保证观察者不能扣除足够的噪声或提前取得未扰动结果。

现有 TeX 已有受信协调者的角色描述。选择协调者加噪方案时，需要把该角色的信任条件、云不能获得私钥和无噪声结果的条件写完整并实现；不能假定当前单进程 SEAL 调用已经满足这些条件。

两条路径仍可动态选择计算位置、聚合位置、加噪位置和保护路径。若明确要求完整公开训练记录满足 DP，纯 HE 可作为单独的保密对照，不作为同等输出 DP 保证的替代项。

## 先降低真实训练维度

首先制作固定低维参数化的诊断分支，可从公开预训练骨干加分类器开始，再测试分布在端侧和边缘侧的低秩适配器。训练和发布的是这些参数，所有非私密公开的基础参数保持冻结。

不能只给少量参数加噪，却继续上传其他经过私有数据训练的参数。数据相关的参数选择、投影基或秩也不能未经保护直接公开；第一版采用预先固定的结构。

只训练分类器可能削弱原七种模式的计算差异，因此它只用于先验证 DP 路径。正式适配器应覆盖需要训练的端侧与边缘侧模块，并重新核算切分通信和计算成本。所有比较方法采用同一个参数化和初始化。

低维方案是有依据的候选，不保证 100 客户端下必然收敛。不能为了报告好精度采用虚拟参与人数降低实际噪声。

## 隐私分析需要闭合的流程

1. 先在没有跨客户端私有状态传播的固定 FedAvg 路径上验证客户端级裁剪与发布机制，使用公开固定参与集合与权重。
2. 对每轮所有云可见对象列出输出，包括单独的 DP 包、允许解密的聚合结果、模式选择和其他数据相关元信息。
3. 为每个输出界定实际敏感度。扩展到共享边缘状态时追踪影响范围，必要时裁剪并保护整个依赖组；不能继续无条件套用单客户端权重。
4. 证明给定先前公开记录的条件下，每个新发布满足对应 DP 界，再组合会计。云只看到密文的部分另外使用密码协议的模拟安全论证，不能直接把真实 CKKS 密文说成标准意义的零 epsilon 发布。
5. 私密随机数、受限解密与角色隔离完成后，核对公开模型的 DP 与协议条件。原始诊断日志仅用于内部研究。

每种模式完成这些条件后才能加入动态搜索。基于私有范数或训练表现的公开选择，也要有会计或改用公开信息、已有 DP 输出。

## 首个验证实验

固定真实 100 客户端、100 轮预算、epsilon=8 和 delta=0.00001。先运行短程，不缩短校准周期。

同一个固定拓扑和参数化下比较无保护、真实 HE、逐包 DP、HE 配合聚合 DP。记录实际可训练维度、发布敏感度、每坐标噪声标准差、信号与噪声范数、损失、精度及解密误差。

先回答低维聚合 DP 是否确实产生可用更新，再扩展模式和动态选择。最终应分别验证协议与会计正确性，以及训练效用，不能仅凭两轮精度上升通过验收。

## 两个备选方向

如果实际需求是保护一条记录或一个人的数据，而不是整台设备的所有数据，可以重新设计为样本级或用户级 DP。需要修改邻接关系、逐样本或逐用户梯度处理和会计，实现后重新实验。不能直接将末端更新敏感度除以本地样本数。

如果坚持 DP 与纯 HE 可互换，可以将目标明确限定为相对于云计算方的规定可见信息保护，并分别证明 DP 发布与 HE 协议相对允许泄露的安全性。此时整个模型没有统一 DP 保证，预算不足转 HE 可以作为条件分支。该路线更贴近现有候选集，但改变了论文可声称的保证，不解决 DP 分支自身的高维效用问题。

## 文献依据及适用限制

[Heikkila，TMLR 2025 作者全文](https://arxiv.org/html/2407.19286v2) 第 3.2 节式 4 给出客户端级裁剪与安全聚合噪声的参照，第 3.3 节说明信任条件。可据此设计标准路径；其结论不直接覆盖本仓库任意边缘私有状态共享。

[DP-DyLoRA，2024 作者稿](https://arxiv.org/html/2405.06368v2) 讨论高维参数更新的 DP 效用问题及参数高效微调。第 V-F 节明确使用大群体噪声模拟，因此不能照搬其大群体结果作为本仓库真实 100 客户端的保证。

[Robust-HDP，ICML 2024 作者全文](https://arxiv.org/html/2406.03519v2) 明确采用记录级邻接与本地 DP-SGD，其噪声与聚合研究可作为备选方向的参考。它不是保持当前客户端级邻接不变的直接替代算法。

本方案建议先验证统一客户端级目标下的低维聚合 DP。若该方向仍无法满足任务需求，再根据实际保护对象决定是否调整粒度，而不是根据最终曲线反推隐私定义。
