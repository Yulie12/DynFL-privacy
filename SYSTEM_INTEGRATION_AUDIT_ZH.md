# 方法2系统接入审计

当前固定发布会计模块已接入诊断，尚未接入正式动态执行器。旧实验保持可复现，不直接修改其结果含义。

## 已接通统一配置入口

用户选择先接通固定真实HE和聚合DP。新增 `configs/method2_fixed_he_dp_cifar10.json`，通过原 `run_paper_config.py` 的独立协议分支执行。旧v29校验不变，新协议不声称使用旧TeX包级校准。当前仅支持 `fixed_secure_aggregate`，拒绝尚未接入的ours、random等策略以及profiled HE，防止错误标记结果。

```powershell
D:\soft\Python310\python.exe experiments\run_paper_config.py --config configs\method2_fixed_he_dp_cifar10.json --seeds 42 --max-new-rounds 2
```

移除 `--max-new-rounds 2` 后按配置执行100轮。短程运行保留100轮隐私周期，但当前不生成可续跑检查点，`--resume-from-run` 会明确拒绝。不要混用旧协议检查点。

已通过统一入口两轮真实训练，精度8.5%、11.5%，与同条件诊断一致；7项新配置和会计测试通过。结果位于 `out/method2_launcher_smoke/2026-09-13_13-57-23_edge_dp_trajectory/report.json`。

该入口仍复用固定训练引擎，不等于动态执行器、网页和完整论文系统已完成接入；不标记为Ours。后续动态协作接入仍需处理参与集合和模式语义，固定保护机制不变。

## 接入发现

`dynfed/selection.py` 的 `update_mechanism_options` 默认为dp、he3、dp_he3，旧候选估计基于链路保护。新方案的全局输出必须满足客户端级DP，因此纯HE不能作为预算耗尽后的替代。直接沿用旧机制标签和成本将产生选择与执行不一致。

可以先固定真实HE和全局聚合DP，验证动态协作执行；这一步不能宣称已有动态隐私选择。另一条路线是新增边缘独立DP发布与跨边缘HE聚合DP发布的联合选择，每条路径均满足输出保护，但敏感度、噪声分配和可见结果不同，必须重新建模。此处已向用户请求确认。

## 模式兼容边界

### 独立执行器已验证的部分

新增 `dynfed/independent_execution.py`，允许LIIC完整本地计算与LIEIIC受信边缘拆分计算，其他模式拒绝。这里复用的是本地计算实现，不代表原传输路径和成本模型已兼容。两种路径都重载上一轮发布模型并重建优化器，不保留跨客户端优化状态。

`--execution-layout` 支持local、trusted_split、alternating_audit，统一配置也可通过training.execution_layout传递。alternating_audit按公开轮次和客户端编号交替，专门验证切换，不是Ours或动态资源优化算法。

完整/拆分更新一致性、跨客户端缓存隔离、输入发布状态不变等检查通过。100客户端两轮混合真实HE回归完成，每轮两种路径各50客户端，随后下一轮交换，精度8.5%、11.5%，与固定路径一致。结果位于 `out/method2_mixed_execution_smoke/2026-09-13_14-05-14_edge_dp_trajectory/report.json`。相关9项测试通过。

选择器尚未接入。原成本模型仍包含包级隐私选择、共享边缘循环和可能变化的admitted set；不能直接把原候选分数应用到已修改的执行路径。

| 原模式 | 方法2接入要求 |
| --- | --- |
| LIIC | 完整独立更新先保护，禁止原始更新明文直达云；新传输路径须计入成本 |
| LIEIIC | 仅限受信边缘计算，客户端模型副本独立，不共享未保护训练状态 |
| LIEIIIC、LIIEIIIC | 禁止私有组聚合回传后继续训练；改成本地独立多步并不等于原层级聚合语义 |
| LIE、LIIE | 本地私有状态不能影响其他客户端或未经重新分析进入后续全局发布；保留与重置规则尚待实现 |
| LIC | 不可信云直接读取embedding不受最终更新DP追溯保护，不能默认沿用 |

该表是接入要求，不表示这些路径已经实现或通过测试。

## 选择信息

### 选择目标兼容性阻塞

接入旧Pareto选择器前发现，当前已验证配置固定head、参与集合、聚合权重、本地步数、裁剪和全局DP发布；local与trusted_split仅改变计算位置，已有更新一致性测试和真实HE混合回归支持其数值近似等价。因此当前两条路径没有已证实的模式相关精度目标差异，不能沿用原选择器根据feature/client-update/edge-update事件数构造不同误差项。

`selection.py` 的 `_local_omega_components` 和 `_cached_local_omega_components` 仍根据旧链路事件分别计算偏差及方差，尚不适配每轮单次全局DP发布。若在当前条件下重新按实际发布建模，隐私相关项对两种位置相同。给两者人为不同的误差惩罚或多样性奖励不符合真实执行。

可先实现仅按真实资源与通信成本调度的系统接入验证，但应标为放置调度验证而非论文完整Pareto方法。若要恢复论文中的精度与时延权衡，需要明确允许什么协作变量实际改变训练或参与，并重新检查敏感度。这不是编码错误，不能静默决定研究目标。

当前正式训练器的常规候选枚举读取资源、样本计数及会计状态，特征裁剪估计使用预设值。`accuracy_oracle` 路径会读取私有训练和验证数据评估候选，只能作为内部诊断，不能作为未经额外保护的部署策略。其余路径仍需完整追踪；尚不能宣称公开决策的DP已经闭合。

## 会计限制

`IndependentReleaseAccount` 只接受固定参与集合和公共计数。选择器改变admitted set时不能直接绕开检查，必须先构造对相邻输入固定的公共计划并按实际权重校准。不能在原私有共享训练循环末尾加一次该模块便宣布完成接入。
