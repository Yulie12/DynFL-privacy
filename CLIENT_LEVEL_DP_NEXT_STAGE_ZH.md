# 客户端级 DP 下一阶段核对

核对日期为 2026 年 9 月 11 日。

## 已确定的范围

使用 CIFAR，直接按现有数据分配规则划分客户端，不在客户端内构造模拟用户。保护单位是一个客户端的全部本地数据，不将其等同于一个自然人的全部数据。

目标同时包含单个更新的传输保护与可见发布结果的 DP。纯 HE 不会自动使解密后的结果满足 DP。实验日志中的已记录事件预算也不是完整训练的隐私证明。

## 已确认的邻接定义

用户已选择 A，保留现有 TeX 和代码的客户端替换邻接，不承诺隐藏设备参与身份。

- 替换定义固定设备名单，比较一个客户端全部本地数据发生变化的情形。
- 加入或移除定义还需要明确空贡献、参与名单、归一化分母及可见元数据。不能仅把敏感度从 2C 改成 C。

保留现有替换定义和噪声参数。

## 当前代码事实

1. `dynfed/split_learning.py` 的更新 DP 路径采用整体裁剪及替换敏感度 2C，拒绝把逐张量裁剪直接视为同一整体敏感度。边缘整包与客户端独立贡献不能混为一谈。
2. `dynfed/privacy.py` 的 RDP 记账使用非子采样高斯机制，噪声乘子定义为标准差除以敏感度。该记账本身不能证明实际敏感度、事件覆盖或数据相关决策成立。
3. 原先只有独立的保护规则审计。本次在正式候选过滤中新增 `update_protection_goal`，启用 `released_model_dp` 时要求每条云端更新链路包含 DP。执行前再次检查选择或复用的候选及已记录事件预算。该门控只是必要覆盖条件，不提供共享状态的完整敏感度证明。
4. `dynfed/fmnist_lenet5_dynamic.py` 会把共享边缘模型返回多个客户端。一个客户端可能影响多个后续包，不能无条件使用独立贡献下的平均敏感度收益。
5. 当前执行审计仍报告完整 DP 未建立。真实 CKKS 运算不等于密钥角色隔离，profiled HE 则只模拟开销。

## 不依赖邻接选择的后续验证

先用固定公开初始模型、固定权重和参与集合做单轮独立贡献对照，比较无保护、逐包 DP、聚合结果 DP。明确实验中的受信边界，聚合噪声必须在非受信方看到结果之前加入。

记录裁剪比例、信号与噪声范数、敏感度、噪声标准差、实际发布次数和测试精度。未加保护的诊断指标只作为内部实验日志。

单轮验证不能代替七种模式的多轮证明。通过后还需核对共享状态、策略选择、所有可见发布及 HE 解密协议，之后再开始正式长实验。本次没有改变数据分配、噪声校准或 TeX，未启动精度训练。

## 新开关的使用边界

直接训练入口增加 `--update-protection-goal released_model_dp`。JSON 配置可在 `privacy` 中加入 `"update_protection_goal": "released_model_dp"`，配置启动器会传递该值。

当前门控仅支持受信端边域。云端更新只能包含 DP 或 DP+HE；仍可按模式选择计算路径，不强制两种机制都出现。使用 DP+HE 需要候选参数包含 `dp_he3`，门控不会自行补充用户未允许的候选。

无保护和纯 HE 对照需要单独以 `packet_protection` 运行，不能标成结果 DP 方法。旧配置未设置该字段时仍采用旧行为，不能把旧结果当成启用了新目标。新目标应新建输出目录运行，不以旧的未保护训练前缀宣称完整 DP。

决策日志增加 `update_protection_goal` 字段。现有完整 DP 未建立的审计状态继续保留，不能根据这个字段宣称安全证明完成。
