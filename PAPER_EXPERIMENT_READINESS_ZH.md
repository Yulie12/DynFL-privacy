# 正式补充实验启动门槛

更新于2026年9月13日。当前状态为未就绪，不能把固定独立客户端诊断改名为Ours后提交。

## 已确认的推进方向

用户已确认以第二条路线作为实验主线。每轮从此前公开DP模型独立训练，各客户端完整可训练贡献裁剪，跨客户端聚合只发生在该轮贡献完成之后；噪声在受信边界内、解密发布之前加入。原论文结构仅作为兼容性审计，不要求退回原共享训练流程。

这不等于七种模式已经兼容。LIEIIIC和LIIEIIIC的私有共享状态回传后继续训练不能直接保留；改成独立本地多步也不能仍宣称原层级聚合语义。LIC把embedding交给诚实但好奇云执行的路径亦需单独检查，更新DP不能追溯保护已暴露的embedding。边缘本地模式的私有状态若影响后续全球发布，也必须纳入依赖分析。尚不直接启用这些模式的正式实验。

## 已完成验证

- Head固定架构下，seed40、42、44各100轮真实SEAL和聚合DP验证完成。
- 完整私钥由指定受信边缘保管，云端使用独立密文进程；已测试错误聚合、轮次重放和单包替换拒绝。
- 低维适配模块已验证，当前没有优于head的证据。
- 正式极端标签分区、12000训练样本、2000测试样本和客户端20%留出已接入可选诊断入口，旧默认不变。

## 必须先解决

正式数据条件的20轮配对诊断已完成，seed42、head、C0.1、100轮会计周期。无保护41.65%（loss1.7139），仅裁剪28.45%（loss2.0250），分布式DP和真实HE14.70%（loss2.6718，epsilon3.3146）。结果位于 `out/head_formal_data_20rounds/2026-09-13_08-46-22_edge_dp_trajectory/report.json`。这里仍固定独立训练，并未接入动态选择。不能将此前IID诊断的46.67%当作正式非IID精度。极端分区下裁剪和噪声均影响学习，尚未达到正式批量运行门槛。

1. 协作依赖与敏感度。采用已确认的独立贡献路线实现新执行路径并同步论文，原路径保留复现。不能只修改模式名字或敏感度数值而保留私有状态共享。
2. 动态决策可见信息。需确认选择器使用和公开的统计是否来自公开资源、此前DP结果或经过单独保护的私有统计，不能只保护模型而遗漏决策信息。
3. 隐私机制定位。最终结果DP不可被纯HE替代；正式候选可行性和对照组标记必须与这一要求一致。不能为展示混合比例强行选择不满足目标的机制。
4. 实验效用。正式非IID条件必须重新验证，旧IID诊断精度不能作为正式条件有效的证据。
5. TeX对应。可训练范围、信任边界、敏感度、会计事件、模式执行及时间口径需与最终冻结版本对应。

## 解锁后的执行顺序

### 本地1轮对照

仅将本地训练3轮改为1轮，其余保持head、正式分区、C0.1、学习率0.01及100轮隐私周期，完成20轮三条轨迹。无保护与仅裁剪均为18.95%（loss2.1778），DP和真实HE为11.85%（loss2.9453，epsilon3.3146），低于原3轮的41.65%、28.45%、14.70%。该对照没有支持采用本地1轮作为默认。

第20轮DP轨迹客户端裁剪前更新范数均值约0.05，DP裁剪比例0，全局信号范数0.00845、噪声范数1.035。固定通信轮数下本地计算量同时减少，不构成客户端漂移影响的纯因果验证。当前还应区分本地优化器梯度裁剪与发布前DP更新裁剪，不能只调后者。

结果位于 `out/head_formal_data_local1_20rounds/2026-09-13_10-05-43_edge_dp_trajectory/report.json`。保持正式默认不变，未放宽预算，仍未达到正式实验启动门槛。

### 裁剪阈值补充对照

### Adapter正式分区对照及信号日志

在相同正式分区、seed42、20轮、C0.1条件下，adapter无保护41.55%、仅裁剪28.45%、真实HE和DP17.85%（loss2.4662）。对应head为41.65%、28.45%、14.70%。单种子的3.15个百分点差异不能证明稳定优势，更不能认定已找到合格工作点。

新增逐轮内部诊断包括裁剪前范数均值和最大值、裁剪比例、全局裁剪前后信号范数、裁剪偏差、噪声范数及噪声信号比。使用加权向量再取范数，不对边缘范数求平均。噪声范数计算为加权加噪包与独立重算信号之差，包含微小浮点求和残差，不包含HE解密误差；控制组出现接近机器精度的小值不代表加了DP噪声。

第20轮adapter裁剪前均值0.2802，裁剪比例100%，全局信号范数0.0130，噪声范数1.4238，比例109.55。不能以比例大于1作为通用不可训练阈值。这些私有诊断不参与选择器，也不作为受保护的公开结果。

结果位于 `out/adapter_formal_data_20rounds/2026-09-13_09-23-08_edge_dp_trajectory/report.json`。105项相关测试通过。尚未修改正式配置或宣称动态协议已闭合。

### Head裁剪阈值对照结果

在同一正式数据条件与seed42下，将C从0.1提高到0.3，预算和会计周期保持不变，真实HE路径完成20轮。仅裁剪恢复到41.65%（loss1.7139），而DP和HE最终13.75%（loss4.7667）。云端噪声标准差按敏感度同步从0.014460提高到0.043380，累计epsilon仍3.3146。该对照未解决效用问题，不采用C0.3作为默认。说明单独放宽裁剪不能同时解决偏斜条件下的裁剪损失和噪声损失，不代表所有低维训练参数均不可用。

结果位于 `out/head_formal_data_clip03_20rounds/2026-09-13_09-14-28_edge_dp_trajectory/report.json`。更新后的诊断日志会明确标注独立贡献协议和未启用动态选择；此次运行启动在日志字段修改前，原报告不追溯改写。

1. 冻结配置与版本，全部方法做短程可行性和隐私日志检查，不混用旧版本结果。
2. 主结果各方法采用同一训练范围、分区、预训练、优化器和随机种子集合，从头独立运行。
3. 决策时间与规模、策略更新周期、隐私预算和消融分别只改目标变量，保存所有配置。
4. 小规模精确解与近似解验证、真实HE数值误差及墙钟分项验证。
5. 第二数据集验证。具体覆盖和种子数对照评审要求后定稿，不预先宣称已经满足。
6. 汇总原始曲线、均值和样本标准差，明确模拟时延与实际墙钟时间，更新图表、可复现参数与TeX。

本清单不是正式启动授权。结构方向已确认，执行协议、模式兼容和非IID效用门槛仍未全部通过。
