# 按保护位置选择 DP 与 HE

用户提出的方向是对所有需要保护的位置配置 DP 或 HE，并对使用方式施加规则。这可以作为动态协作重构的一部分。成立条件取决于对象、观察者与允许公开的结果，不取决于两种机制是否都使用 epsilon。

## 两个不同的要求

| 要求 | DP | HE |
| --- | --- | --- |
| 云端只能拿到受扰动的单个更新，或通过安全聚合仅获得允许公开的总和 | 有效的逐包 DP 可以提供相应的统计保护 | 密钥与解密限制成立时，可隐藏超出允许总和的单个输入信息 |
| 观察者拿到最终模型后仍有指定的客户端级 DP 界 | 需要对整个可见发布记录核对 DP 与组合会计 | 纯 HE 不提供解密结果的 DP 界，需另行加入满足该目标的扰动 |

第一行描述两条可接受但保证不同的路径，不把它们称为相同强度的保护。HE 不会阻止从协议允许公开的总和推断信息。例如，若一个聚合结果只包含一个参与者，它本身就是该参与者的更新。

前期发布的 DP 结果不会因后来使用 HE 而失去自身的 DP 属性。但包含后续未按 DP 校准的公开结果的完整记录，不能直接沿用前期的总 epsilon。进一步访问原始训练数据计算出的结果，也不能仅因使用了先前的 DP 模型就被当作免费后处理。

## 建议的铺设规则

| 位置 | 当前对象和接收方 | 规则 |
| --- | --- | --- |
| 端与其关联边缘之间 | embedding、logits、反向梯度、内部更新 | 当前 TeX 的受信域内部，依赖该信任假设。它不是这些对象已满足 DP 的声明 |
| 端到云或边缘到云 | 单客户端更新或边缘更新包 | 逐包 DP，或真实 HE 与受限聚合协议，或二者组合 |
| 聚合结果与后续全局模型发布 | 云端、客户端或其他模型接收者可见的结果 | 单独说明允许公开什么。若声明输出 DP，每次新的数据相关发布都要纳入相应分析 |
| 解密过程 | 密文、解密请求、密钥持有方 | 云计算方不能持有可解密全部单个包的私钥。仅允许经验证的聚合解密，限制串谋及通过多个聚合结果消去他人贡献 |

传输加密还需要身份认证与完整性保护。DP 并非传输加密，HE 的聚合运算本身也不能代替全部通信安全措施。

## 预算用完后怎么办

每次决策都先检查下一次 DP 发布是否仍满足总预算，而不是等浮点剩余额度恰好变成零。

若目标是上述单个更新保护，且安全聚合协议可用，预算不足后可以只保留 HE，再检查资源、覆盖和时延约束。若 HE 也不可行，该次发布需要等待、停止或调整协作方式，不能退回裸明文。

若目标要求发布结果继续满足同一个 DP 界，纯 HE 不能替代下一次数据相关发布的 DP。可以提前分配隐私消耗或减少数据相关发布；预算不足后停止该客户端对外发布新的数据相关贡献。只有在对应依赖分析成立时，才能据此继续其他客户端的训练。

预算用完后切换只是预算约束触发的一个分支。真正的动态选择应在每次策略更新时比较当前所有满足规则的候选，预算仍充足时也可以因为计算、通信和效用代价选择 HE。

## 本次实现

`dynfed/protection_rules.py` 提供条件化的候选规则检查与实际发布审计。规则检查依赖 DP 分析有效和安全聚合协议可用这两个前提，不是本仓库已有完整安全证明的声明。

训练器生成 `protection_releases.csv`，逐包记录来源、云端目标、客户端集合、模式、保护机制、加噪位置、DP 预算检查和真实 HE 运算状态。`round_metrics.csv` 还记录哪些贡献没有对应的 DP 校准。

当前运行使用单进程角色与实验随机种子，日志中的 `key_isolation_enforced` 和 `aggregate_only_decryption_enforced` 为 false。`real_he_pending_role_isolation` 表示执行了密码运算，但尚未执行隔离协议。

新增规则用于验证设计与审计实际执行，尚未接管选择器，也没有改变正式候选集合、预算、信任域或训练方法。DP 的高噪声效用问题和共享边缘状态下的敏感度问题仍待解决。

建议将论文目标明确为针对观察者的协作保护要求，并分别报告 DP 事件消耗与 HE 协议条件。如果还要求最终模型满足统一 DP，应将它作为另一个明确要求并继续验证对应机制。

## 近期依据

[DDFed，2024](https://www.sciencedirect.com/science/article/pii/S004579062400168X) 的出版社摘要描述了 DP 与 HE 的不同保护路径及分类聚合。本次仅据已获得的摘要与摘录引用其架构，不能据此声称相同的完整模型 DP 保证。

[Heikkila，TMLR 2025 作者全文](https://arxiv.org/html/2407.19286v2) 区分限制计算可见信息的安全聚合与限制输出推断的 DP，并明确讨论信任假设。这支持按观察者和发布对象核对组合机制，不能替代本仓库自身的敏感度与协议分析。
