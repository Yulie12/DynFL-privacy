*** Begin Patch
*** Update File: dynfed/fmnist_lenet5_dynamic.py
@@
-            "local_steps": selection.L_block_cycles,
+            # L_block_cycles is a system/logical cost parameter, not a training
+            # optimizer step limit. Mainline fusion must execute the requested
+            # local epochs completely.
+            "local_steps": (
+                None
+                if selection.mainline_fusion
+                else selection.L_block_cycles
+            ),
@@
-                local_steps=selection_config.L_block_cycles,
+                # Do not use logical communication cycles to truncate local
+                # optimization in the mainline fusion training path.
+                local_steps=(
+                    None
+                    if selection_config.mainline_fusion
+                    else selection_config.L_block_cycles
+                ),
*** Update File: tests/test_fusion_epoch_execution.py
@@
+# Add/update the regression assertion so that mainline fusion keeps
+# local_steps=None and executes full local epochs.
+# (Adapt this assertion to the existing test helper names.)
*** End Patch
